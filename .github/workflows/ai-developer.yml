name: AI Developer

on:
  issue_comment:
    types: [created]
  pull_request_review:
    types: [submitted]

permissions:
  contents: read

jobs:
  gate-issue-entry:
    name: Gate Issue entry
    if: >-
      github.event_name == 'issue_comment' &&
      github.event.issue.pull_request == null &&
      github.event.issue.state == 'open' &&
      contains(fromJSON('["OWNER","MEMBER","COLLABORATOR"]'), github.event.comment.author_association) &&
      contains(fromJSON('["OWNER","MEMBER","COLLABORATOR"]'), github.event.issue.author_association) &&
      !contains(github.event.issue.labels.*.name, 'human-review-required') &&
      (
        github.event.comment.body == '/codex develop' ||
        github.event.comment.body == '/codex develop extended'
      )
    runs-on: ubuntu-latest
    permissions:
      contents: read
      issues: read
      pull-requests: read
    outputs:
      continue: ${{ steps.pause-gate.outputs.continue }}
    steps:
      - name: Check out trusted entry gate
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          persist-credentials: false

      - name: Reject a paused Issue or related PR
        id: pause-gate
        env:
          GH_TOKEN: ${{ github.token }}
          ISSUE_NUMBER: ${{ github.event.issue.number }}
        shell: bash
        run: |
          set -euo pipefail
          result="$(bash .github/scripts/evaluate-issue-entry-gate.sh "$GITHUB_REPOSITORY" "$ISSUE_NUMBER")"
          echo "continue=$(jq -r .continue <<< "$result")" >> "$GITHUB_OUTPUT"
          jq -r .reason <<< "$result"

  develop-from-issue:
    name: Develop from Issue
    needs: gate-issue-entry
    if: needs.gate-issue-entry.outputs.continue == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: ${{ github.event.comment.body == '/codex develop extended' && 35 || 15 }}
    concurrency:
      group: codex-issue-${{ github.event.issue.number }}
      cancel-in-progress: false
    outputs:
      pre_write_remote_head: ${{ steps.remote-head.outputs.head }}
    steps:
      - name: Create developer App token
        id: dev-token
        uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1 # v3.2.0
        with:
          client-id: ${{ vars.DEV_APP_CLIENT_ID }}
          private-key: ${{ secrets.DEV_APP_PRIVATE_KEY }}

      - name: Check out repository
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          token: ${{ steps.dev-token.outputs.token }}
          fetch-depth: 0

      - name: Capture pre-write remote branch HEAD
        id: remote-head
        env:
          GH_TOKEN: ${{ steps.dev-token.outputs.token }}
          ISSUE_NUMBER: ${{ github.event.issue.number }}
        shell: bash
        run: |
          set -euo pipefail
          [[ "$ISSUE_NUMBER" =~ ^[1-9][0-9]*$ ]]
          branch="ai/issue-${ISSUE_NUMBER}"
          head="$(python3 - "$GITHUB_REPOSITORY" "$branch" <<'PY'
          import json
          import os
          import re
          import sys
          import urllib.error
          import urllib.request

          repo, branch = sys.argv[1:]
          headers = {"Authorization": "Bearer " + os.environ["GH_TOKEN"],
                     "Accept": "application/vnd.github+json"}

          def get(path):
              request = urllib.request.Request("https://api.github.com" + path, headers=headers)
              with urllib.request.urlopen(request, timeout=20) as response:
                  return json.load(response)

          repository = get("/repos/" + repo)
          if repository.get("full_name") != repo:
              raise ValueError("repository identity mismatch")
          try:
              result = get("/repos/" + repo + "/git/ref/heads/" + branch)
          except urllib.error.HTTPError as error:
              if error.code != 404:
                  raise
              print("absent")
              sys.exit(0)
          head = result.get("object", {}).get("sha")
          if (result.get("ref") != "refs/heads/" + branch
                  or result.get("object", {}).get("type") != "commit"
                  or not isinstance(head, str)
                  or not re.fullmatch(r"[0-9a-f]{40}", head)):
              raise ValueError("malformed remote branch response")
          print(head)
          PY
          )"
          printf 'head=%s\n' "$head" >> "$GITHUB_OUTPUT"

      - name: Prepare branch and Issue context
        id: issue_context
        env:
          GH_TOKEN: ${{ steps.dev-token.outputs.token }}
          ISSUE_NUMBER: ${{ github.event.issue.number }}
        shell: bash
        run: |
          set -euo pipefail
          base_sha="$(git rev-parse HEAD)"
          notify_human_blob="$(git rev-parse "${base_sha}:.github/scripts/notify-human.sh")"
          apply_human_pause_blob="$(git rev-parse "${base_sha}:.github/scripts/apply-human-pause.sh")"
          requirements_marker_blob="$(git rev-parse "${base_sha}:.github/scripts/has-requirements-change-marker.sh")"
          diff_guard_blob="$(git rev-parse "${base_sha}:.github/scripts/evaluate-codex-diff-gate.sh")"
          development_context_blob="$(git rev-parse "${base_sha}:.github/scripts/build-development-context.py")"
          pause_helpers=(create-human-pause human-pause-record list-human-pause-records
            validate-human-pause-record-graph decompose-human-pause-record-graph
            derive-human-pause-pre-resume-state reconcile-human-pause-resume-acceptance
            reconcile-human-pause-active-pause apply-human-pause
            format-human-pause-notification notify-human)
          pause_helper_blobs=()
          for helper in "${pause_helpers[@]}"; do
            pause_helper_blobs+=("$(git rev-parse "${base_sha}:.github/scripts/${helper}.sh")")
          done
          {
            printf 'base_sha=%s\n' "$base_sha"
            printf 'notify_human_blob=%s\n' "$notify_human_blob"
            printf 'apply_human_pause_blob=%s\n' "$apply_human_pause_blob"
            printf 'requirements_marker_blob=%s\n' "$requirements_marker_blob"
            printf 'diff_guard_blob=%s\n' "$diff_guard_blob"
            printf 'pause_helper_blobs=%s\n' "${pause_helper_blobs[*]}"
          } >> "$GITHUB_OUTPUT"
          branch="ai/issue-${ISSUE_NUMBER}"
          if git fetch origin "refs/heads/${branch}:refs/remotes/origin/${branch}" 2>/dev/null; then
            git checkout -B "$branch" "origin/$branch"
          else
            git checkout -b "$branch"
          fi
          echo "AI_BRANCH=$branch" >> "$GITHUB_ENV"
          git show "${base_sha}:.github/scripts/notify-human.sh" > "$RUNNER_TEMP/notify-human.sh"
          git show "${base_sha}:.github/scripts/apply-human-pause.sh" > "$RUNNER_TEMP/apply-human-pause.sh"
          git show "${base_sha}:.github/scripts/has-requirements-change-marker.sh" > "$RUNNER_TEMP/has-requirements-change-marker.sh"
          git show "${base_sha}:.github/scripts/evaluate-codex-diff-gate.sh" > "$RUNNER_TEMP/evaluate-codex-diff-gate.sh"
          git show "${base_sha}:.github/scripts/build-development-context.py" > "$RUNNER_TEMP/build-development-context.py"
          test "$(git hash-object --no-filters "$RUNNER_TEMP/notify-human.sh")" = "$notify_human_blob"
          test "$(git hash-object --no-filters "$RUNNER_TEMP/apply-human-pause.sh")" = "$apply_human_pause_blob"
          test "$(git hash-object --no-filters "$RUNNER_TEMP/has-requirements-change-marker.sh")" = "$requirements_marker_blob"
          test "$(git hash-object --no-filters "$RUNNER_TEMP/evaluate-codex-diff-gate.sh")" = "$diff_guard_blob"
          test "$(git hash-object --no-filters "$RUNNER_TEMP/build-development-context.py")" = "$development_context_blob"
          bash "$RUNNER_TEMP/evaluate-codex-diff-gate.sh" --contract > "$RUNNER_TEMP/codex-diff-guard-contract.json"
          jq -e '
            type == "object" and
            (keys | sort == ["max_changed_files", "max_changed_lines", "max_new_files"]) and
            (.max_changed_files | type == "number" and . > 0 and floor == .) and
            (.max_changed_lines | type == "number" and . > 0 and floor == .) and
            (.max_new_files | type == "number" and . > 0 and floor == .)
          ' "$RUNNER_TEMP/codex-diff-guard-contract.json" > /dev/null

          mkdir -p .ai-context
          git show "${base_sha}:AGENTS.md" > .ai-context/AGENTS.base.md
          cp "$RUNNER_TEMP/codex-diff-guard-contract.json" .ai-context/diff-guard-contract.json
          gh issue view "$ISSUE_NUMBER" --repo "$GITHUB_REPOSITORY" \
            --json number,title,body,url,labels,comments \
            > "$RUNNER_TEMP/development-issue.json"
          python3 "$RUNNER_TEMP/build-development-context.py" \
            "$RUNNER_TEMP/development-issue.json" .ai-context/request.md "$GITHUB_STEP_SUMMARY"

          # These pre-Codex copies are disposable bootstrap inputs. The Codex
          # workload receives RUNNER_TEMP, so no helper retained there across
          # model execution is trusted for a post-Codex decision.
          rm -f -- \
            "$RUNNER_TEMP/notify-human.sh" \
            "$RUNNER_TEMP/apply-human-pause.sh" \
            "$RUNNER_TEMP/has-requirements-change-marker.sh" \
            "$RUNNER_TEMP/evaluate-codex-diff-gate.sh" \
            "$RUNNER_TEMP/build-development-context.py" \
            "$RUNNER_TEMP/development-issue.json" \
            "$RUNNER_TEMP/codex-diff-guard-contract.json"

      - name: Validate Codex model configuration
        env:
          CODEX_MODEL: ${{ vars.CODEX_MODEL }}
        shell: bash
        run: |
          set -euo pipefail
          if [[ ! "$CODEX_MODEL" =~ [^[:space:]] ]]; then
            echo 'CODEX_MODEL repository variable must contain a non-whitespace value.' >&2
            exit 1
          fi

      - name: Prepare Codex developer runtime
        env:
          CODEX_HOME: ${{ runner.temp }}/codex-home
          CODEX_FINAL: ${{ runner.temp }}/codex-final.md
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p "$CODEX_HOME"
          rm -f "$CODEX_HOME/config.toml"
          rm -f "$CODEX_FINAL"

      - name: Setup Codex developer runtime
        uses: openai/codex-action@86365089eb2b84e0a8fb0717b304f8bdcb13b20e # v1.12
        with:
          openai-api-key: ${{ secrets.OPENAI_API_KEY }}
          codex-version: 0.156.1
          codex-home: ${{ runner.temp }}/codex-home
          safety-strategy: unsafe

      - name: Resolve trusted Codex developer runtime
        id: codex_runtime
        timeout-minutes: 3
        env:
          CODEX_HOME: ${{ runner.temp }}/codex-home
        shell: bash
        run: |
          set -euo pipefail

          launcher="$(command -v codex)"
          test -n "$launcher"
          entry="$(realpath "$launcher")"
          test "$(basename "$entry")" = codex.js
          package_root="$(realpath "$(dirname "$(dirname "$entry")")")"
          test -s "$package_root/package.json"
          test "$(realpath "$package_root/bin/codex.js")" = "$entry"

          export CODEX_ENTRY="$entry"
          export CODEX_PACKAGE_ROOT="$package_root"
          resolver_output="$(
            node --input-type=module <<'NODE'
          import fs from "node:fs";
          import path from "node:path";
          import { createRequire } from "node:module";

          const entry = process.env.CODEX_ENTRY;
          const packageRoot = process.env.CODEX_PACKAGE_ROOT;
          const mainPackage = JSON.parse(
            fs.readFileSync(path.join(packageRoot, "package.json"), "utf8"),
          );
          if (mainPackage.name !== "@openai/codex") {
            throw new Error(`unexpected package name: ${mainPackage.name}`);
          }
          if (mainPackage.version !== "0.156.1") {
            throw new Error(`unexpected package version: ${mainPackage.version}`);
          }

          let platformPackage;
          let targetTriple;
          if (process.platform === "linux" && process.arch === "x64") {
            platformPackage = "@openai/codex-linux-x64";
            targetTriple = "x86_64-unknown-linux-musl";
          } else if (process.platform === "linux" && process.arch === "arm64") {
            platformPackage = "@openai/codex-linux-arm64";
            targetTriple = "aarch64-unknown-linux-musl";
          } else {
            throw new Error(
              `unsupported Codex platform: ${process.platform}/${process.arch}`,
            );
          }

          const require = createRequire(entry);
          const platformPackageJson = require.resolve(
            `${platformPackage}/package.json`,
          );
          const nativePath = path.join(
            path.dirname(platformPackageJson),
            "vendor",
            targetTriple,
            "bin",
            "codex",
          );
          const nativeStat = fs.statSync(nativePath);
          if (!nativeStat.isFile()) {
            throw new Error("native codex is not a regular file");
          }
          fs.accessSync(nativePath, fs.constants.X_OK);

          process.stdout.write(
            JSON.stringify({
              nativePath: fs.realpathSync(nativePath),
              packageRoot,
              targetTriple,
            }),
          );
          NODE
          )"

          native_path="$(jq -r '.nativePath' <<<"$resolver_output")"
          resolved_root="$(jq -r '.packageRoot' <<<"$resolver_output")"
          target_triple="$(jq -r '.targetTriple' <<<"$resolver_output")"
          test "$resolved_root" = "$package_root"
          test -x "$native_path"

          launcher_version="$(codex --version)"
          native_version="$(
            env \
              -u CODEX_MANAGED_BY_BUN \
              -u CODEX_MANAGED_BY_PNPM \
              -u CODEX_MANAGED_BY_VITE_PLUS \
              CODEX_MANAGED_PACKAGE_ROOT="$package_root" \
              CODEX_MANAGED_BY_NPM=1 \
              "$native_path" --version
          )"
          test "$launcher_version" = "$native_version"
          test "$native_version" = 'codex-cli 0.156.1'

          action_root="$(dirname "$RUNNER_WORKSPACE")/_actions/openai/codex-action/86365089eb2b84e0a8fb0717b304f8bdcb13b20e"
          action_main="$action_root/dist/main.js"
          test -f "$action_main"
          actual_blob="$(git hash-object "$action_main")"
          test "$actual_blob" = ce4e94e119abb91b980d23bfb4210688241f3a0a

          uid="$(id -u)"
          gid="$(id -g)"
          groups_json="$(id -G | tr ' ' '\n' | jq -Rsc 'split("\n") | map(select(length > 0) | tonumber)')"
          credentials="$(jq -cn \
            --argjson uid "$uid" \
            --argjson gid "$gid" \
            --argjson groups "$groups_json" \
            '{userId:$uid,primaryGroupId:$gid,supplementaryGroupIds:$groups}')"

          printf 'native_path=%s\n' "$native_path" >> "$GITHUB_OUTPUT"
          printf 'package_root=%s\n' "$package_root" >> "$GITHUB_OUTPUT"
          printf 'action_main=%s\n' "$action_main" >> "$GITHUB_OUTPUT"
          printf 'runner_credentials=%s\n' "$credentials" >> "$GITHUB_OUTPUT"
          printf 'Resolved trusted Codex 0.156.1 runtime for %s (Action blob %s).\n' \
            "$target_triple" "$actual_blob"

      - name: Prepare fixed Codex developer prompt
        timeout-minutes: 3
        env:
          CODEX_PROMPT_FILE: ${{ runner.temp }}/codex-developer-prompt.md
        shell: bash
        run: |
          set -euo pipefail
          cat > "$CODEX_PROMPT_FILE" <<'CODEX_PROMPT'
          Read .ai-context/AGENTS.base.md, .ai-context/request.md, and .ai-context/diff-guard-contract.json completely. The base-commit AGENTS copy is the governing instruction file.
          Implement the Issue in this working tree.
          The trusted contract sets maximum changed files, total changed lines (additions + deletions), and new files; keep every metric at or below its maximum.
          Keep the proposed repository change within the trusted diff guard contract. Avoid broad formatting changes and large generated additions. If the Issue cannot fit within those bounds, keep the change small and explain the required split in the final response.
          Do not commit, push, open a pull request, merge, or contact external services; the workflow handles GitHub writes after you finish.
          CODEX_PROMPT

      - name: Capture AI Developer host integrity baseline
        id: host_integrity_before
        timeout-minutes: 1
        shell: bash
        run: |
          set -euo pipefail

          notify_before="$(stat -Lc '%d %i %u %g %a' /run/systemd/notify)"
          dbus_before="$(stat -Lc '%d %i %u %g %a' /run/dbus/system_bus_socket)"
          resolved_before="$(
            systemctl show systemd-resolved.service --no-pager \
              --property=ActiveState --property=SubState --property=MainPID --property=NRestarts |
              LC_ALL=C sort |
              tr '\n' ';'
          )"
          printf '%s\n' "$resolved_before" | tr ';' '\n' | grep -Fxq 'ActiveState=active'
          printf '%s\n' "$resolved_before" | tr ';' '\n' | grep -Fxq 'SubState=running'
          getent ahosts github.com >/dev/null
          getent ahosts api.github.com >/dev/null

          printf 'notify=%s\n' "$notify_before" >> "$GITHUB_OUTPUT"
          printf 'dbus=%s\n' "$dbus_before" >> "$GITHUB_OUTPUT"
          printf 'resolved=%s\n' "$resolved_before" >> "$GITHUB_OUTPUT"
          printf 'HOST_INTEGRITY before sockets=captured resolved=active/running dns=ok\n'

      - name: Run Codex developer
        id: codex
        timeout-minutes: ${{ github.event.comment.body == '/codex develop extended' && 30 || 12 }}
        env:
          CODEX_HOME: ${{ runner.temp }}/codex-home
          CODEX_FINAL: ${{ runner.temp }}/codex-final.md
          CODEX_PROMPT_FILE: ${{ runner.temp }}/codex-developer-prompt.md
          CODEX_MODEL: ${{ vars.CODEX_MODEL }}
          CODEX_INTERNAL_ORIGINATOR_OVERRIDE: codex_github_action
          CODEX_NATIVE: ${{ steps.codex_runtime.outputs.native_path }}
          CODEX_PACKAGE_ROOT: ${{ steps.codex_runtime.outputs.package_root }}
          ACTION_MAIN: ${{ steps.codex_runtime.outputs.action_main }}
          RUNNER_CREDENTIALS: ${{ steps.codex_runtime.outputs.runner_credentials }}
          CODEX_RUNTIME_MAX_SEC: ${{ github.event.comment.body == '/codex develop extended' && 1780 || 700 }}
        shell: bash
        run: |
          set -euo pipefail

          test -x "$CODEX_NATIVE"
          test -f "$ACTION_MAIN"
          test "$(git hash-object "$ACTION_MAIN")" = ce4e94e119abb91b980d23bfb4210688241f3a0a
          test -s "$CODEX_HOME/config.toml"
          test -s "$CODEX_PROMPT_FILE"

          python3 - "$CODEX_HOME/config.toml" <<'PY'
          import pathlib
          import sys
          import tomllib
          import urllib.parse

          config_path = pathlib.Path(sys.argv[1])
          with config_path.open("rb") as fh:
              config = tomllib.load(fh)

          allowed_top_level = {"model_provider", "model_providers"}
          unexpected = sorted(set(config) - allowed_top_level)
          if unexpected:
              raise SystemExit(
                  f"unexpected Codex config keys in setup-only runtime: {unexpected}"
              )

          provider_name = config.get("model_provider")
          if provider_name != "codex-action-responses-proxy":
              raise SystemExit("unexpected Codex model_provider")

          providers = config.get("model_providers")
          if not isinstance(providers, dict) or set(providers) != {provider_name}:
              raise SystemExit("unexpected Codex model_providers shape")
          provider = providers[provider_name]
          if not isinstance(provider, dict):
              raise SystemExit("unexpected Codex provider config")
          if provider.get("wire_api") != "responses":
              raise SystemExit("Codex proxy provider must use responses wire API")

          parsed = urllib.parse.urlparse(str(provider.get("base_url", "")))
          if (
              parsed.scheme != "http"
              or parsed.hostname != "127.0.0.1"
              or parsed.port is None
              or not (1 <= parsed.port <= 65535)
              or parsed.path != "/v1"
              or parsed.params
              or parsed.query
              or parsed.fragment
              or parsed.username is not None
              or parsed.password is not None
          ):
              raise SystemExit("Codex proxy provider must be loopback http /v1")
          PY

          case "$CODEX_RUNTIME_MAX_SEC" in
            700|1780) ;;
            *)
              echo 'Unexpected Codex RuntimeMaxSec.' >&2
              exit 1
              ;;
          esac

          uid="$(id -u)"
          gid="$(id -g)"
          groups_json="$(id -G | tr ' ' '\n' | jq -Rsc 'split("\n") | map(select(length > 0) | tonumber)')"
          current_credentials="$(jq -cn \
            --argjson uid "$uid" \
            --argjson gid "$gid" \
            --argjson groups "$groups_json" \
            '{userId:$uid,primaryGroupId:$gid,supplementaryGroupIds:$groups}')"
          test "$current_credentials" = "$RUNNER_CREDENTIALS"
          test "$uid" -ne 0

          nobody_gid="$(id -g nobody)"
          case "$nobody_gid" in
            ''|*[!0-9]*|0)
              echo 'Could not resolve a safe nobody gid.' >&2
              exit 1
              ;;
          esac

          runner_user="$(id -un)"
          runner_home="$HOME"
          runner_path="$PATH"
          unit="codex-developer-${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}"

          cat > "$RUNNER_TEMP/run-native-codex.sh" <<'CODEX_RUN'
          #!/bin/sh
          set -eu

          expected_uid="${1:?expected uid is required}"
          expected_gid="${2:?expected gid is required}"

          actual_uid="$(/usr/bin/id -u)"
          if [ "$actual_uid" != "$expected_uid" ]; then
            echo "Service-local hardening preflight UID mismatch: expected $expected_uid, got $actual_uid." >&2
            exit 41
          fi

          actual_gid="$(/usr/bin/id -g)"
          if [ "$actual_gid" != "$expected_gid" ]; then
            echo "Service-local hardening preflight GID mismatch: expected $expected_gid, got $actual_gid." >&2
            exit 42
          fi

          groups_status="$(/usr/bin/awk '/^Groups:/ { $1=""; sub(/^[[:space:]]+/, ""); print }' /proc/self/status)"
          if [ -n "$groups_status" ]; then
            echo "Service-local hardening preflight retained supplementary groups: $groups_status." >&2
            exit 43
          fi

          no_new_privs="$(/usr/bin/awk '/^NoNewPrivs:/ { print $2 }' /proc/self/status)"
          if [ "$no_new_privs" != 1 ]; then
            echo "Service-local hardening preflight NoNewPrivs mismatch: got ${no_new_privs:-missing}." >&2
            exit 44
          fi

          for field in CapInh CapPrm CapEff CapBnd CapAmb; do
            value="$(/usr/bin/awk -v field="$field" '$1 == field ":" { print $2 }' /proc/self/status)"
            if [ "$value" != 0000000000000000 ]; then
              echo "Service-local hardening preflight capability is nonzero: $field=${value:-missing}." >&2
              exit 45
            fi
          done

          if [ ! -x /usr/bin/sudo ]; then
            echo 'Service-local hardening preflight cannot verify sudo rejection: /usr/bin/sudo is unavailable.' >&2
            exit 39
          fi
          if /usr/bin/sudo -n true >/dev/null 2>&1; then
            echo 'Service-local hardening preflight unexpectedly retained sudo.' >&2
            exit 40
          fi

          /usr/bin/python3 - <<'PY'
          import errno
          import os
          import socket
          import stat
          import sys

          protected_paths = os.environ.get("PROTECTED_UNIX_SOCKET_PATHS", "").split()
          if (
              len(protected_paths) != 13
              or len(set(protected_paths)) != 13
              or any(not path.startswith("/run/") for path in protected_paths)
          ):
              print(
                  "Service-local hardening preflight protected socket allowlist is malformed.",
                  file=sys.stderr,
              )
              raise SystemExit(48)

          try:
              sock = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
          except OSError as exc:
              print(
                  f"Service-local hardening preflight blocks AF_UNIX required by Codex sandbox: {exc}",
                  file=sys.stderr,
              )
              raise SystemExit(46)
          else:
              sock.close()

          try:
              sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
          except OSError as exc:
              print(
                  f"Service-local hardening preflight blocks AF_INET: {exc}",
                  file=sys.stderr,
              )
              raise SystemExit(47)
          else:
              sock.close()

          host_ids = {}
          raw_host_ids = os.environ.get("PROTECTED_UNIX_SOCKET_HOST_IDS", "")
          if raw_host_ids:
              try:
                  for item in raw_host_ids.split(";"):
                      path, devino = item.split("=", 1)
                      dev, ino = (int(part) for part in devino.split(":", 1))
                      if path in host_ids:
                          raise ValueError("duplicate protected socket host id")
                      host_ids[path] = (dev, ino)
              except (TypeError, ValueError):
                  print(
                      "Service-local hardening preflight protected socket host baseline is malformed.",
                      file=sys.stderr,
                  )
                  raise SystemExit(48)
          if any(path not in protected_paths for path in host_ids):
              print(
                  "Service-local hardening preflight protected socket host baseline has an unexpected path.",
                  file=sys.stderr,
              )
              raise SystemExit(48)

          for path in protected_paths:
              try:
                  st = os.stat(path)
              except FileNotFoundError:
                  continue
              except OSError as exc:
                  print(
                      f"Service-local hardening preflight cannot inspect protected socket {path}: {exc}",
                      file=sys.stderr,
                  )
                  raise SystemExit(48)

              if path not in host_ids:
                  print(
                      f"Service-local hardening preflight protected socket appeared after host baseline: {path}.",
                      file=sys.stderr,
                  )
                  raise SystemExit(48)

              if (
                  not stat.S_ISSOCK(st.st_mode)
                  or stat.S_IMODE(st.st_mode) != 0
                  or os.access(path, os.R_OK)
                  or os.access(path, os.W_OK)
                  or os.access(path, os.X_OK)
                  or (st.st_dev, st.st_ino) == host_ids[path]
              ):
                  print(
                      f"Service-local hardening preflight protected socket mask is ineffective: {path}.",
                      file=sys.stderr,
                  )
                  raise SystemExit(48)

          def handle_walk_error(exc):
              if exc.errno in (errno.EACCES, errno.EPERM, errno.ENOENT):
                  return
              raise exc

          residual = []
          try:
              for root, _dirs, files in os.walk(
                  "/run",
                  topdown=True,
                  onerror=handle_walk_error,
                  followlinks=False,
              ):
                  for name in files:
                      path = os.path.join(root, name)
                      try:
                          st = os.stat(path, follow_symlinks=False)
                      except (FileNotFoundError, PermissionError):
                          continue
                      if (
                          stat.S_ISSOCK(st.st_mode)
                          and st.st_uid == 0
                          and os.access(path, os.W_OK)
                      ):
                          residual.append(path)
          except OSError as exc:
              print(
                  f"Service-local hardening preflight failed while scanning /run: {exc}",
                  file=sys.stderr,
              )
              raise SystemExit(49)

          if residual:
              rendered = ", ".join(sorted(set(residual))[:20])
              print(
                  "Service-local hardening preflight found writable root-owned UNIX socket(s): "
                  + rendered,
                  file=sys.stderr,
              )
              raise SystemExit(49)

          print(
              "Service-local hardening preflight verified AF_UNIX/AF_INET and protected UNIX socket boundary."
          )
          PY

          exec env \
            -u PROTECTED_UNIX_SOCKET_PATHS \
            -u PROTECTED_UNIX_SOCKET_HOST_IDS \
            -u CODEX_MANAGED_BY_BUN \
            -u CODEX_MANAGED_BY_PNPM \
            -u CODEX_MANAGED_BY_VITE_PLUS \
            CODEX_MANAGED_PACKAGE_ROOT="$CODEX_PACKAGE_ROOT" \
            CODEX_MANAGED_BY_NPM=1 \
            CODEX_HOME="$CODEX_HOME" \
            CODEX_INTERNAL_ORIGINATOR_OVERRIDE="$CODEX_INTERNAL_ORIGINATOR_OVERRIDE" \
            "$CODEX_NATIVE" exec \
              --skip-git-repo-check \
              --cd "$GITHUB_WORKSPACE" \
              --output-last-message "$CODEX_FINAL" \
              --model "$CODEX_MODEL" \
              --config 'model_reasoning_effort="medium"' \
              --config 'default_permissions=":workspace"' \
              < "$CODEX_PROMPT_FILE"
          CODEX_RUN
          chmod 700 "$RUNNER_TEMP/run-native-codex.sh"

          exec sudo -n -- \
            env -u ENV -u BASH_ENV -u SHELLOPTS \
            /bin/sh -c '
              set -eu
              runner_user="$1"
              uid="$2"
              nobody_gid="$3"
              runner_home="$4"
              runner_path="$5"
              runner_temp="$6"
              runtime_max_sec="$7"
              unit="$8"
              launcher="$9"
              code_workspace="${10}"
              codex_home="${11}"
              codex_final="${12}"
              codex_prompt_file="${13}"
              codex_model="${14}"
              codex_native="${15}"
              codex_package_root="${16}"
              originator="${17}"
              protected_unix_socket_paths="/run/dbus/system_bus_socket /run/dhcpcd/eth0-4.unpriv.sock /run/docker.sock /run/snapd-snap.socket /run/snapd.socket /run/systemd/io.systemd.ManagedOOM /run/systemd/journal/dev-log /run/systemd/journal/socket /run/systemd/journal/stdout /run/systemd/journal/syslog /run/systemd/notify /run/systemd/userdb/io.systemd.DynamicUser /run/uuidd/request"
              inaccessible_paths=""
              protected_unix_socket_host_ids=""
              for path in $protected_unix_socket_paths; do
                inaccessible_paths="${inaccessible_paths:+$inaccessible_paths }-$path"
                if [ -e "$path" ]; then
                  if [ ! -S "$path" ]; then
                    echo "Service-local hardening root preflight protected UNIX socket baseline failed: $path is not a socket." >&2
                    exit 50
                  fi
                  if ! host_owner="$(/usr/bin/stat -Lc "%u" "$path")"; then
                    echo "Service-local hardening root preflight protected UNIX socket baseline failed: could not stat owner for $path." >&2
                    exit 50
                  fi
                  if ! host_devino="$(/usr/bin/stat -Lc "%d:%i" "$path")"; then
                    echo "Service-local hardening root preflight protected UNIX socket baseline failed: could not stat dev:inode for $path." >&2
                    exit 50
                  fi
                  if [ "$host_owner" != 0 ]; then
                    echo "Service-local hardening root preflight protected UNIX socket baseline failed: $path owner=$host_owner." >&2
                    exit 50
                  fi
                  protected_unix_socket_host_ids="${protected_unix_socket_host_ids}${protected_unix_socket_host_ids:+;}${path}=${host_devino}"
                fi
              done

              set +e
              /usr/bin/systemd-run \
                --quiet \
                --wait \
                --collect \
                --unit="$unit" \
                --property=Type=exec \
                --property="RuntimeMaxSec=${runtime_max_sec}s" \
                --property=TimeoutStopSec=5s \
                --property=KillMode=control-group \
                --property=SendSIGKILL=yes \
                --property=NoNewPrivileges=yes \
                --property="InaccessiblePaths=$inaccessible_paths" \
                --property=SystemCallArchitectures=native \
                --property="SystemCallFilter=~io_uring_setup io_uring_enter io_uring_register" \
                /usr/bin/setpriv \
                  --reuid="$uid" \
                  --regid="$nobody_gid" \
                  --clear-groups \
                  --no-new-privs \
                  --bounding-set=-all \
                  --inh-caps=-all \
                  --ambient-caps=-all \
                  -- /usr/bin/env -i \
                  "HOME=$runner_home" \
                  "USER=$runner_user" \
                  "LOGNAME=$runner_user" \
                  "PATH=$runner_path" \
                  "RUNNER_TEMP=$runner_temp" \
                  "GITHUB_WORKSPACE=$code_workspace" \
                  "CODEX_HOME=$codex_home" \
                  "CODEX_FINAL=$codex_final" \
                  "CODEX_PROMPT_FILE=$codex_prompt_file" \
                  "CODEX_MODEL=$codex_model" \
                  "CODEX_NATIVE=$codex_native" \
                  "CODEX_PACKAGE_ROOT=$codex_package_root" \
                  "CODEX_INTERNAL_ORIGINATOR_OVERRIDE=$originator" \
                  "PROTECTED_UNIX_SOCKET_PATHS=$protected_unix_socket_paths" \
                  "PROTECTED_UNIX_SOCKET_HOST_IDS=$protected_unix_socket_host_ids" \
                  /bin/sh "$launcher" "$uid" "$nobody_gid"
              rc=$?
              set -e

              /usr/bin/journalctl \
                --unit="$unit" \
                --no-pager \
                --output=cat \
                --lines=200 || true

              if [ "$rc" -eq 0 ]; then
                expected_preflight_marker="Service-local hardening preflight verified AF_UNIX/AF_INET and protected UNIX socket boundary."
                set +e
                preflight_journal="$(
                  /usr/bin/journalctl \
                    --unit="$unit" \
                    --no-pager \
                    --output=cat \
                    --quiet
                )"
                journal_rc=$?
                set -e
                if [ "$journal_rc" -ne 0 ] || ! printf "%s\n" "$preflight_journal" | grep -Fxq "$expected_preflight_marker"; then
                  echo "Service-local hardening preflight success marker unavailable from unit journal." >&2
                  exit 51
                fi
                printf "%s\n" "$expected_preflight_marker"
              fi
              exit "$rc"
            ' codex-developer \
            "$runner_user" \
            "$uid" \
            "$nobody_gid" \
            "$runner_home" \
            "$runner_path" \
            "$RUNNER_TEMP" \
            "$CODEX_RUNTIME_MAX_SEC" \
            "$unit" \
            "$RUNNER_TEMP/run-native-codex.sh" \
            "$GITHUB_WORKSPACE" \
            "$CODEX_HOME" \
            "$CODEX_FINAL" \
            "$CODEX_PROMPT_FILE" \
            "$CODEX_MODEL" \
            "$CODEX_NATIVE" \
            "$CODEX_PACKAGE_ROOT" \
            "$CODEX_INTERNAL_ORIGINATOR_OVERRIDE"

      - name: Verify AI Developer host integrity
        if: always()
        timeout-minutes: 1
        env:
          HOST_NOTIFY_BEFORE: ${{ steps.host_integrity_before.outputs.notify }}
          HOST_DBUS_BEFORE: ${{ steps.host_integrity_before.outputs.dbus }}
          HOST_RESOLVED_BEFORE: ${{ steps.host_integrity_before.outputs.resolved }}
        shell: bash
        run: |
          set -euo pipefail

          test -n "$HOST_NOTIFY_BEFORE"
          test -n "$HOST_DBUS_BEFORE"
          test -n "$HOST_RESOLVED_BEFORE"

          notify_after="$(stat -Lc '%d %i %u %g %a' /run/systemd/notify)"
          dbus_after="$(stat -Lc '%d %i %u %g %a' /run/dbus/system_bus_socket)"
          test "$notify_after" = "$HOST_NOTIFY_BEFORE"
          test "$dbus_after" = "$HOST_DBUS_BEFORE"

          resolved_after="$(
            systemctl show systemd-resolved.service --no-pager \
              --property=ActiveState --property=SubState --property=MainPID --property=NRestarts |
              LC_ALL=C sort |
              tr '\n' ';'
          )"
          test "$resolved_after" = "$HOST_RESOLVED_BEFORE"
          printf '%s\n' "$resolved_after" | tr ';' '\n' | grep -Fxq 'ActiveState=active'
          printf '%s\n' "$resolved_after" | tr ';' '\n' | grep -Fxq 'SubState=running'
          getent ahosts github.com >/dev/null
          getent ahosts api.github.com >/dev/null
          printf 'HOST_INTEGRITY after sockets=unchanged resolved=unchanged dns=ok\n'

      - name: Restore trusted post-Codex helpers
        env:
          BASE_SHA: ${{ steps.issue_context.outputs.base_sha }}
          NOTIFY_HUMAN_BLOB: ${{ steps.issue_context.outputs.notify_human_blob }}
          APPLY_HUMAN_PAUSE_BLOB: ${{ steps.issue_context.outputs.apply_human_pause_blob }}
          REQUIREMENTS_MARKER_BLOB: ${{ steps.issue_context.outputs.requirements_marker_blob }}
          DIFF_GUARD_BLOB: ${{ steps.issue_context.outputs.diff_guard_blob }}
          PAUSE_HELPER_BLOBS: ${{ steps.issue_context.outputs.pause_helper_blobs }}
        shell: bash
        run: |
          set -euo pipefail

          restore_base_blob() {
            local source_path="${1:?source path is required}"
            local destination="${2:?destination is required}"
            local expected_blob="${3:?expected blob is required}"
            if [[ ! "$expected_blob" =~ ^[0-9a-f]{40}$ ]]; then
              echo "Invalid trusted helper blob identity for $source_path." >&2
              exit 1
            fi
            rm -f -- "$destination"
            git show "${BASE_SHA}:${source_path}" > "$destination"
            actual_blob="$(git hash-object --no-filters "$destination")"
            if [ "$actual_blob" != "$expected_blob" ]; then
              echo "Trusted helper blob changed across Codex execution: $source_path" >&2
              exit 1
            fi
          }

          restore_base_blob '.github/scripts/notify-human.sh' \
            "$RUNNER_TEMP/notify-human.sh" "$NOTIFY_HUMAN_BLOB"
          restore_base_blob '.github/scripts/apply-human-pause.sh' \
            "$RUNNER_TEMP/apply-human-pause.sh" "$APPLY_HUMAN_PAUSE_BLOB"
          restore_base_blob '.github/scripts/has-requirements-change-marker.sh' \
            "$RUNNER_TEMP/has-requirements-change-marker.sh" "$REQUIREMENTS_MARKER_BLOB"
          restore_base_blob '.github/scripts/evaluate-codex-diff-gate.sh' \
            "$RUNNER_TEMP/evaluate-codex-diff-gate.sh" "$DIFF_GUARD_BLOB"

          pause_helpers=(create-human-pause human-pause-record list-human-pause-records
            validate-human-pause-record-graph decompose-human-pause-record-graph
            derive-human-pause-pre-resume-state reconcile-human-pause-resume-acceptance
            reconcile-human-pause-active-pause apply-human-pause
            format-human-pause-notification notify-human)
          read -r -a expected_blobs <<< "$PAUSE_HELPER_BLOBS"
          [ "${#expected_blobs[@]}" -eq "${#pause_helpers[@]}" ] || exit 1
          trusted_pause_dir="$RUNNER_TEMP/trusted-human-pause"
          rm -rf -- "$trusted_pause_dir"
          mkdir -p "$trusted_pause_dir"
          for index in "${!pause_helpers[@]}"; do
            helper="${pause_helpers[$index]}"
            restore_base_blob ".github/scripts/${helper}.sh" \
              "$trusted_pause_dir/${helper}.sh" "${expected_blobs[$index]}"
          done

          rm -f -- "$RUNNER_TEMP/codex-diff-guard-contract.json"
          bash "$RUNNER_TEMP/evaluate-codex-diff-gate.sh" --contract > "$RUNNER_TEMP/codex-diff-guard-contract.json"
          jq -e '
            type == "object" and
            (keys | sort == ["max_changed_files", "max_changed_lines", "max_new_files"]) and
            (.max_changed_files | type == "number" and . > 0 and floor == .) and
            (.max_changed_lines | type == "number" and . > 0 and floor == .) and
            (.max_new_files | type == "number" and . > 0 and floor == .)
          ' "$RUNNER_TEMP/codex-diff-guard-contract.json" > /dev/null
          echo 'TRUSTED_POST_CODEX_HELPERS restored base blobs and regenerated diff guard contract.'

      - name: Gate requirement changes
        id: development-gate
        env:
          GH_TOKEN: ${{ steps.dev-token.outputs.token }}
          ISSUE_NUMBER: ${{ github.event.issue.number }}
          CODEX_FINAL: ${{ runner.temp }}/codex-final.md
          APP_SLUG: ${{ steps.dev-token.outputs.app-slug }}
          NOTIFICATION_WEBHOOK_URL: ${{ secrets.NOTIFICATION_WEBHOOK_URL }}
        shell: bash
        run: |
          set -euo pipefail
          pause_for_human() {
            local pause_reason="${1:?pause reason is required}"
            local detail="${2:?pause detail is required}"
            local app_id fingerprint branch pr_list pr_number
            [[ "$ISSUE_NUMBER" =~ ^[1-9][0-9]*$ ]]
            branch="ai/issue-${ISSUE_NUMBER}"
            pr_list="$(gh pr list --repo "$GITHUB_REPOSITORY" --head "$branch" --state open --json number,headRefName,isCrossRepository --limit 100)"
            pr_number="$(jq -er --arg branch "$branch" '
              if type != "array" or length >= 100 or
                 (all(.[];
                   type == "object" and
                   (.number | type == "number" and . > 0 and floor == .) and
                   .headRefName == $branch and
                   (.isCrossRepository | type == "boolean")) | not)
              then error("malformed or incomplete PR list")
              else
                map(select(.isCrossRepository == false)) |
                if length > 1 then error("ambiguous PR target")
                elif length == 0 then "-"
                else .[] | .number | tostring end
              end
            ' <<< "$pr_list")"
            app_id="$(gh api "/apps/$APP_SLUG" --jq '.id')"
            [[ "$app_id" =~ ^[1-9][0-9]*$ ]]
            local options=(--failed-action develop)
            if [ "$pause_reason" = requirements_change ]; then
              fingerprint="$(gh api "/repos/$GITHUB_REPOSITORY/issues/$ISSUE_NUMBER" | python3 -c '
          import hashlib, json, sys
          body = json.load(sys.stdin)["body"]
          if not isinstance(body, str):
              raise ValueError("Issue body is not a string")
          print("sha256:" + hashlib.sha256(body.encode("utf-8")).hexdigest())
          ')"
              options=(--issue-body-fingerprint "$fingerprint")
            fi
            bash "$RUNNER_TEMP/trusted-human-pause/create-human-pause.sh" create \
              "$GITHUB_REPOSITORY" "$ISSUE_NUMBER" "${pr_number:--}" "$app_id" \
              "$pause_reason" "$detail" "${options[@]}"
            gh issue comment "$ISSUE_NUMBER" --repo "$GITHUB_REPOSITORY" --body "$detail"
            echo 'continue=false' >> "$GITHUB_OUTPUT"
          }
          if [ ! -s "$CODEX_FINAL" ]; then
            pause_for_human developer_execution_failed 'Codex final response is missing; automated development is paused pending a human decision.'
            exit 0
          fi
          set +e
          bash "$RUNNER_TEMP/has-requirements-change-marker.sh" "$CODEX_FINAL"
          marker_status=$?
          set -e
          if [ "$marker_status" -gt 1 ]; then
            pause_for_human developer_execution_failed 'Requirements-change marker helper failed; automated development is paused pending a human decision.'
            exit 0
          elif [ "$marker_status" -eq 0 ]; then
            pause_for_human requirements_change 'Codex detected that a requirements change is required. Automated development is paused for a human decision.'
          else
            echo 'continue=true' >> "$GITHUB_OUTPUT"
          fi

      - name: Evaluate trusted diff guard
        if: steps.development-gate.outputs.continue == 'true'
        id: diff-guard
        env:
          GH_TOKEN: ${{ steps.dev-token.outputs.token }}
          ISSUE_NUMBER: ${{ github.event.issue.number }}
          APP_SLUG: ${{ steps.dev-token.outputs.app-slug }}
          NOTIFICATION_WEBHOOK_URL: ${{ secrets.NOTIFICATION_WEBHOOK_URL }}
        shell: bash
        run: |
          set -euo pipefail
          rm -rf .ai-context
          git reset -- .ai-context
          git add -A

          if ! jq -e '
            type == "object" and
            (keys | sort == ["max_changed_files", "max_changed_lines", "max_new_files"]) and
            (.max_changed_files | type == "number" and . > 0 and floor == .) and
            (.max_changed_lines | type == "number" and . > 0 and floor == .) and
            (.max_new_files | type == "number" and . > 0 and floor == .)
          ' "$RUNNER_TEMP/codex-diff-guard-contract.json" > /dev/null; then
            echo 'Trusted diff guard contract is unavailable or malformed.' >&2
            exit 1
          fi
          max_changed_files="$(jq -r .max_changed_files "$RUNNER_TEMP/codex-diff-guard-contract.json")"
          max_changed_lines="$(jq -r .max_changed_lines "$RUNNER_TEMP/codex-diff-guard-contract.json")"
          max_new_files="$(jq -r .max_new_files "$RUNNER_TEMP/codex-diff-guard-contract.json")"

          result_json=''
          helper_status=0
          set +e
          result_json="$(bash "$RUNNER_TEMP/evaluate-codex-diff-gate.sh")"
          helper_status=$?
          set -e

          parsed=false
          result=''
          if [ -n "$result_json" ] && jq -e '
            type == "object" and
            (.result | type == "string") and
            (.changed_files | type == "number" and . >= 0 and floor == .) and
            (.additions | type == "number" and . >= 0 and floor == .) and
            (.deletions | type == "number" and . >= 0 and floor == .) and
            (.total_changed_lines | type == "number" and . >= 0 and floor == .) and
            (.new_files | type == "number" and . >= 0 and floor == .)
          ' <<< "$result_json" > /dev/null 2>&1; then
            parsed=true
            result="$(jq -r .result <<< "$result_json")"
          fi

          if [ "$helper_status" -eq 0 ] && [ "$parsed" = true ] && [ "$result" = pass ]; then
            echo 'continue=true' >> "$GITHUB_OUTPUT"
            {
              echo '### AI Developer diff guard'
              echo
              echo '- Result: pass'
              echo "- Changed files: $(jq -r .changed_files <<< "$result_json") / ${max_changed_files}"
              echo "- Additions: $(jq -r .additions <<< "$result_json")"
              echo "- Deletions: $(jq -r .deletions <<< "$result_json")"
              echo "- Total changed lines: $(jq -r .total_changed_lines <<< "$result_json") / ${max_changed_lines}"
              echo "- New files: $(jq -r .new_files <<< "$result_json") / ${max_new_files}"
            } >> "$GITHUB_STEP_SUMMARY"
            exit 0
          fi

          reason='Trusted diff guard failed closed before repository write.'
          if [ "$helper_status" -eq 0 ] && [ "$parsed" = true ] && [ "$result" = stop ]; then
            reason='Trusted diff guard stopped an oversized repository change before commit/push/PR write.'
          elif [ "$parsed" = true ] && [ "$result" = error ]; then
            reason='Trusted diff guard could not safely measure the staged change; automated development is paused.'
          elif [ "$parsed" = true ]; then
            reason="Trusted diff guard returned unexpected result '${result}'; automated development is paused."
          elif [ "$helper_status" -ne 0 ]; then
            reason='Trusted diff guard helper failed without a valid machine-readable result; automated development is paused.'
          else
            reason='Trusted diff guard output could not be parsed; automated development is paused.'
          fi

          [[ "$ISSUE_NUMBER" =~ ^[1-9][0-9]*$ ]]
          branch="ai/issue-${ISSUE_NUMBER}"
          pr_list="$(gh pr list --repo "$GITHUB_REPOSITORY" --head "$branch" --state open --json number,headRefName,isCrossRepository --limit 100)"
          pr_number="$(jq -er --arg branch "$branch" '
            if type != "array" or length >= 100 or
               (all(.[];
                 type == "object" and
                 (.number | type == "number" and . > 0 and floor == .) and
                 .headRefName == $branch and
                 (.isCrossRepository | type == "boolean")) | not)
            then error("malformed or incomplete PR list")
            else
              map(select(.isCrossRepository == false)) |
              if length > 1 then error("ambiguous PR target")
              elif length == 0 then "-"
              else .[] | .number | tostring end
            end
          ' <<< "$pr_list")"
          app_id="$(gh api "/apps/$APP_SLUG" --jq '.id')"
          [[ "$app_id" =~ ^[1-9][0-9]*$ ]]
          pause_reason=diff_guard_error
          options=()
          if [ "$helper_status" -eq 0 ] && [ "$parsed" = true ] && [ "$result" = stop ]; then
            pause_reason=diff_guard_exceeded
            fingerprint="$(gh api "/repos/$GITHUB_REPOSITORY/issues/$ISSUE_NUMBER" | python3 -c '
          import hashlib, json, sys
          body = json.load(sys.stdin)["body"]
          if not isinstance(body, str):
              raise ValueError("Issue body is not a string")
          print("sha256:" + hashlib.sha256(body.encode("utf-8")).hexdigest())
          ')"
            options=(--issue-body-fingerprint "$fingerprint")
          fi
          bash "$RUNNER_TEMP/trusted-human-pause/create-human-pause.sh" create \
            "$GITHUB_REPOSITORY" "$ISSUE_NUMBER" "${pr_number:--}" "$app_id" \
            "$pause_reason" "$reason" "${options[@]}"

          diagnostics="$reason"
          if [ "$parsed" = true ]; then
            diagnostics+=$'\n\n'"Result: ${result}."
            if [ "$result" = stop ]; then
              for field in changed_files additions deletions total_changed_lines new_files; do
                value="$(jq -r --arg field "$field" '.[$field]' <<< "$result_json")"
                diagnostics+=$'\n'"${field}: ${value}"
              done
            else
              if [ "$result" = error ]; then
                error_code="$(jq -r 'if (.error | type) == "string" then .error else empty end' <<< "$result_json")"
                if [ -n "$error_code" ]; then
                  diagnostics+=$'\n'"error: ${error_code}"
                fi
              fi
              diagnostics+=$'\n'"Metrics: unavailable"
            fi
          else
            diagnostics+=$'\n\n'"Result: unavailable."
            diagnostics+=$'\n'"Metrics: unavailable"
          fi
          gh issue comment "$ISSUE_NUMBER" --repo "$GITHUB_REPOSITORY" --body "$diagnostics"

          {
            echo '### AI Developer diff guard'
            echo
            echo "- Result: ${result:-unavailable}"
            echo "- Thresholds: ${max_changed_files} changed files / ${max_changed_lines} total changed lines / ${max_new_files} new files"
            if [ "$parsed" = true ] && [ "$result" = stop ]; then
              for field in changed_files additions deletions total_changed_lines new_files; do
                value="$(jq -r --arg field "$field" '.[$field]' <<< "$result_json")"
                echo "- ${field}: ${value}"
              done
            else
              if [ "$parsed" = true ] && [ "$result" = error ]; then
                error_code="$(jq -r 'if (.error | type) == "string" then .error else empty end' <<< "$result_json")"
                if [ -n "$error_code" ]; then
                  echo "- error: ${error_code}"
                fi
              fi
              echo '- Metrics: unavailable'
            fi
            echo '- Decision: repository write blocked'
          } >> "$GITHUB_STEP_SUMMARY"
          echo 'continue=false' >> "$GITHUB_OUTPUT"

      - name: Commit, push, and open or update PR
        if: steps.development-gate.outputs.continue == 'true' && steps.diff-guard.outputs.continue == 'true'
        env:
          GH_TOKEN: ${{ steps.dev-token.outputs.token }}
          APP_SLUG: ${{ steps.dev-token.outputs.app-slug }}
          ISSUE_NUMBER: ${{ github.event.issue.number }}
          ISSUE_TITLE: ${{ github.event.issue.title }}
          CODEX_FINAL: ${{ runner.temp }}/codex-final.md
        shell: bash
        run: |
          set -euo pipefail

          bot_login="${APP_SLUG}[bot]"
          bot_id="$(gh api "/users/${bot_login}" --jq .id)"
          git config user.name "$bot_login"
          git config user.email "${bot_id}+${bot_login}@users.noreply.github.com"

          if git diff --cached --quiet; then
            gh issue comment "$ISSUE_NUMBER" --repo "$GITHUB_REPOSITORY" \
              --body 'No repository change was produced by this AI Developer run. No commit or push was performed. Human confirmation is required.'
            exit 0
          fi

          git commit -m "Implement #${ISSUE_NUMBER} with Codex"
          pushed_commit="$(git rev-parse HEAD)"
          git push --set-upstream origin "$AI_BRANCH"

          report_file="$(mktemp)"
          {
            echo '### Validation provenance'
            echo
            echo '- The validation statements in the Codex report below are Codex-reported results. They are not formal GitHub Actions evidence.'
            echo "- Pushed commit: ${pushed_commit}"
            echo '- Formal current-head validation is recorded separately by GitHub Actions/checks. This AI Developer message does not attest their status or result.'
            echo
            echo '### Codex report'
            echo
            if [ -s "$CODEX_FINAL" ]; then cat "$CODEX_FINAL"; else echo 'Codex completed without a final summary.'; fi
          } > "$report_file"

          pr_list="$(gh pr list --repo "$GITHUB_REPOSITORY" --head "$AI_BRANCH" --state open --json number,headRefName,isCrossRepository --limit 100)"
          pr_number="$(jq -er --arg branch "$AI_BRANCH" '
            if type != "array" or length >= 100 or
               (all(.[];
                 type == "object" and
                 (.number | type == "number" and . > 0 and floor == .) and
                 .headRefName == $branch and
                 (.isCrossRepository | type == "boolean")) | not)
            then error("malformed or incomplete PR list")
            else
              map(select(.isCrossRepository == false)) |
              if length > 1 then error("ambiguous PR target")
              elif length == 0 then "-"
              else .[] | .number | tostring end
            end
          ' <<< "$pr_list")"
          if [ "$pr_number" = - ]; then
            body_file="$(mktemp)"
            {
              echo "Closes #${ISSUE_NUMBER}"
              echo
              echo '## AI development result'
              echo
              cat "$report_file"
              echo
              echo '## Review readiness'
              echo
              echo '- [ ] Related references, terminology, traceability, and diagrams for this decision are updated together within the Issue scope.'
              echo '- [ ] Codex-reported validation has been reviewed as self-reported evidence, and applicable GitHub Actions/checks for the current head have been reviewed separately; failures, unrun validation, and remaining uncertainty are recorded.'
              echo '- [ ] Remaining impacts and follow-up decisions are recorded in the closing Issue.'
              echo '- [ ] A human has checked the complete change and confirmed it is ready for Claude review.'
              echo
              echo 'This PR starts as Draft. Keep related corrections here, then have a human mark it Ready for review. Draft updates do not run Claude review. Follow docs/30_operations/ai-development-workflow.md for readiness checks and paused-review recovery.'
            } > "$body_file"
            pr_url="$(gh pr create --repo "$GITHUB_REPOSITORY" --base main --head "$AI_BRANCH" --draft --title "$ISSUE_TITLE" --body-file "$body_file")"
            rm -f "$body_file"
            gh issue comment "$ISSUE_NUMBER" --repo "$GITHUB_REPOSITORY" --body "Codex opened ${pr_url} as Draft. Check related corrections and validation, then mark it Ready for review to request Claude review."
          else
            gh pr comment "$pr_number" --repo "$GITHUB_REPOSITORY" --body-file "$report_file"
          fi
          rm -f "$report_file"

  handle-issue-developer-failure:
    name: Handle Issue developer failure
    needs: [gate-issue-entry, develop-from-issue]
    if: >-
      always() &&
      github.event_name == 'issue_comment' &&
      github.event.issue.pull_request == null &&
      needs.gate-issue-entry.result == 'success' &&
      needs.gate-issue-entry.outputs.continue == 'true' &&
      needs.develop-from-issue.result != 'success'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      issues: write
      pull-requests: write
    steps:
      - name: Check out trusted failure handler
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          ref: ${{ github.sha }}
          persist-credentials: false

      - name: Create developer App token
        id: dev-token
        uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1 # v3.2.0
        with:
          client-id: ${{ vars.DEV_APP_CLIENT_ID }}
          private-key: ${{ secrets.DEV_APP_PRIVATE_KEY }}

      - name: Create common human pause
        env:
          GH_TOKEN: ${{ steps.dev-token.outputs.token }}
          APP_SLUG: ${{ steps.dev-token.outputs.app-slug }}
          ISSUE_NUMBER: ${{ github.event.issue.number }}
          JOB_RESULT: ${{ needs.develop-from-issue.result }}
          PRE_HEAD: ${{ needs.develop-from-issue.outputs.pre_write_remote_head }}
          NOTIFICATION_WEBHOOK_URL: ${{ secrets.NOTIFICATION_WEBHOOK_URL }}
        shell: bash
        run: |
          set -euo pipefail
          [[ "$ISSUE_NUMBER" =~ ^[1-9][0-9]*$ ]]
          branch="ai/issue-${ISSUE_NUMBER}"
          app_id="$(gh api "/apps/$APP_SLUG" --jq '.id')"
          [[ "$app_id" =~ ^[1-9][0-9]*$ ]]
          pr_list="$(gh pr list --repo "$GITHUB_REPOSITORY" --head "$branch" --state open --json number,headRefName,isCrossRepository --limit 100)"
          pr_number="$(jq -er --arg branch "$branch" '
            if type != "array" or length >= 100 or
               (all(.[];
                 type == "object" and
                 (.number | type == "number" and . > 0 and floor == .) and
                 .headRefName == $branch and
                 (.isCrossRepository | type == "boolean")) | not)
            then error("malformed or incomplete PR list")
            else
              map(select(.isCrossRepository == false)) |
              if length > 1 then error("ambiguous PR target")
              elif length == 0 then "-"
              else .[] | .number | tostring end
            end
          ' <<< "$pr_list")"

          current_head=unknown
          if current="$(python3 - "$GITHUB_REPOSITORY" "$branch" <<'PY'
          import json
          import os
          import re
          import sys
          import urllib.error
          import urllib.request

          repo, branch = sys.argv[1:]
          headers = {"Authorization": "Bearer " + os.environ["GH_TOKEN"],
                     "Accept": "application/vnd.github+json"}

          def get(path):
              request = urllib.request.Request("https://api.github.com" + path, headers=headers)
              with urllib.request.urlopen(request, timeout=20) as response:
                  return json.load(response)

          repository = get("/repos/" + repo)
          if repository.get("full_name") != repo:
              raise ValueError("repository identity mismatch")
          try:
              result = get("/repos/" + repo + "/git/ref/heads/" + branch)
          except urllib.error.HTTPError as error:
              if error.code != 404:
                  raise
              print("absent")
              sys.exit(0)
          head = result.get("object", {}).get("sha")
          if (result.get("ref") != "refs/heads/" + branch
                  or result.get("object", {}).get("type") != "commit"
                  or not isinstance(head, str)
                  or not re.fullmatch(r"[0-9a-f]{40}", head)):
              raise ValueError("malformed remote branch response")
          print(head)
          PY
          )"; then
            current_head="$current"
          fi
          valid_head() { [[ "$1" == absent || "$1" =~ ^[0-9a-f]{40}$ ]]; }
          reason=state_inconsistent
          options=()
          if [[ "$JOB_RESULT" == failure || "$JOB_RESULT" == cancelled ]] &&
             valid_head "$PRE_HEAD" && valid_head "$current_head" &&
             [[ "$PRE_HEAD" == "$current_head" ]]; then
            reason=developer_execution_failed
            options=(--failed-action develop)
          fi
          detail="Issue developer job result: ${JOB_RESULT}; pre-write remote HEAD: ${PRE_HEAD:-missing}; current remote HEAD: ${current_head}. Run: ${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}"
          bash .github/scripts/create-human-pause.sh create \
            "$GITHUB_REPOSITORY" "$ISSUE_NUMBER" "$pr_number" "$app_id" \
            "$reason" "$detail" "${options[@]}"

  draft-after-claude-changes:
    name: Draft after Claude changes requested
    if: >-
      github.event_name == 'pull_request_review' &&
      github.event.review.state == 'changes_requested' &&
      github.event.review.commit_id == github.event.pull_request.head.sha &&
      github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - name: Create reviewer App token for identity verification
        id: review-token
        uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1 # v3.2.0
        with:
          client-id: ${{ vars.REVIEW_APP_CLIENT_ID }}
          private-key: ${{ secrets.REVIEW_APP_PRIVATE_KEY }}

      - name: Verify review author
        id: verify-reviewer
        env:
          EXPECTED_LOGIN: ${{ steps.review-token.outputs.app-slug }}[bot]
          ACTUAL_LOGIN: ${{ github.event.review.user.login }}
        shell: bash
        run: |
          set -euo pipefail
          expected_slug="${EXPECTED_LOGIN%\[bot\]}"
          if [ "$ACTUAL_LOGIN" = "$EXPECTED_LOGIN" ] || [ "$ACTUAL_LOGIN" = "$expected_slug" ]; then
            echo 'trusted=true' >> "$GITHUB_OUTPUT"
          else
            echo "Ignoring change request from untrusted reviewer: $ACTUAL_LOGIN"
            echo 'trusted=false' >> "$GITHUB_OUTPUT"
          fi

      - name: Convert pull request to Draft
        if: steps.verify-reviewer.outputs.trusted == 'true'
        env:
          GH_TOKEN: ${{ steps.review-token.outputs.token }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
        shell: bash
        run: |
          set -euo pipefail
          if [ "$(gh pr view "$PR_NUMBER" --repo "$GITHUB_REPOSITORY" --json isDraft --jq .isDraft)" = false ]; then
            gh pr ready "$PR_NUMBER" --repo "$GITHUB_REPOSITORY" --undo
          fi

  respond-to-claude:
    name: Respond to Claude
    needs: draft-after-claude-changes
    if: >-
      needs.draft-after-claude-changes.result == 'success' &&
      github.event_name == 'pull_request_review' &&
      github.event.review.state == 'changes_requested' &&
      github.event.pull_request.head.repo.full_name == github.repository &&
      startsWith(github.event.pull_request.head.ref, 'ai/issue-')
    runs-on: ubuntu-latest
    timeout-minutes: 30
    concurrency:
      group: codex-pr-${{ github.event.pull_request.number }}
      cancel-in-progress: false
    outputs:
      write_result: ${{ steps.repository-write.outputs.result }}
      ready_done: ${{ steps.ready-traceability.outputs.done }}
      validation_code: ${{ steps.current-validation.outputs.code }}
    steps:
      - name: Create reviewer App token for identity verification
        id: review-token
        uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1 # v3.2.0
        with:
          client-id: ${{ vars.REVIEW_APP_CLIENT_ID }}
          private-key: ${{ secrets.REVIEW_APP_PRIVATE_KEY }}

      - name: Verify review author
        id: verify-reviewer
        env:
          EXPECTED_LOGIN: ${{ steps.review-token.outputs.app-slug }}[bot]
          ACTUAL_LOGIN: ${{ github.event.review.user.login }}
        shell: bash
        run: |
          set -euo pipefail
          expected_slug="${EXPECTED_LOGIN%\[bot\]}"
          if [ "$ACTUAL_LOGIN" = "$EXPECTED_LOGIN" ] || [ "$ACTUAL_LOGIN" = "$expected_slug" ]; then
            echo 'trusted=true' >> "$GITHUB_OUTPUT"
          else
            echo "Ignoring change request from untrusted reviewer: $ACTUAL_LOGIN"
            echo 'trusted=false' >> "$GITHUB_OUTPUT"
          fi

      - name: Create developer App token
        if: steps.verify-reviewer.outputs.trusted == 'true'
        id: dev-token
        uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1 # v3.2.0
        with:
          client-id: ${{ vars.DEV_APP_CLIENT_ID }}
          private-key: ${{ secrets.DEV_APP_PRIVATE_KEY }}

      - name: Check out trusted automation
        if: steps.verify-reviewer.outputs.trusted == 'true'
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          ref: ${{ github.event.pull_request.base.sha }}
          persist-credentials: false

      - name: Gate automated follow-up
        if: steps.verify-reviewer.outputs.trusted == 'true'
        id: followup-gate
        env:
          GH_TOKEN: ${{ steps.dev-token.outputs.token }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          REVIEWER_APP_SLUG: ${{ steps.review-token.outputs.app-slug }}
          DEVELOPER_APP_SLUG: ${{ steps.dev-token.outputs.app-slug }}
          REVIEW_BODY: ${{ github.event.review.body }}
          NOTIFICATION_WEBHOOK_URL: ${{ secrets.NOTIFICATION_WEBHOOK_URL }}
        shell: bash
        run: |
          set -euo pipefail
          result="$(bash .github/scripts/evaluate-followup-gate.sh \
            "$GITHUB_REPOSITORY" "$PR_NUMBER" "$REVIEWER_APP_SLUG" "$DEVELOPER_APP_SLUG" "$REVIEW_BODY")"
          continue="$(jq -r .continue <<< "$result")"
          escalate="$(jq -r .escalate <<< "$result")"
          reason="$(jq -r .reason <<< "$result")"
          pause_code="$(jq -r .pause_code <<< "$result")"

          if [ "$escalate" = 'true' ]; then
            case "$pause_code" in
              round_limit|explicit_human_escalation|state_inconsistent) ;;
              *) echo 'Invalid automated follow-up pause code.' >&2; exit 1 ;;
            esac
            app_id="$(gh api "/apps/$DEVELOPER_APP_SLUG" --jq '.id')"
            bash .github/scripts/create-human-pause.sh create \
              "$GITHUB_REPOSITORY" '-' "$PR_NUMBER" "$app_id" \
              "$pause_code" "$reason"
          fi
          echo "reason=${reason}" >> "$GITHUB_OUTPUT"
          echo "continue=${continue}" >> "$GITHUB_OUTPUT"

      - name: Mark automated follow-up in progress
        if: steps.verify-reviewer.outputs.trusted == 'true' && steps.followup-gate.outputs.continue == 'true'
        env:
          GH_TOKEN: ${{ steps.dev-token.outputs.token }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
        shell: bash
        run: |
          set -euo pipefail
          gh label create ai-followup-in-progress --repo "$GITHUB_REPOSITORY" \
            --color 0E8A16 --description 'Automated follow-up awaiting validated re-review' --force
          gh pr edit "$PR_NUMBER" --repo "$GITHUB_REPOSITORY" --add-label ai-followup-in-progress
          gh pr view "$PR_NUMBER" --repo "$GITHUB_REPOSITORY" --json labels \
            --jq '.labels | any(.name == "ai-followup-in-progress")' | grep -Fxq true

      - name: Check out PR branch
        if: steps.verify-reviewer.outputs.trusted == 'true' && steps.followup-gate.outputs.continue == 'true'
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          token: ${{ steps.dev-token.outputs.token }}
          ref: ${{ github.event.pull_request.head.ref }}
          fetch-depth: 0

      - name: Build review context
        id: followup_context
        if: steps.verify-reviewer.outputs.trusted == 'true' && steps.followup-gate.outputs.continue == 'true'
        env:
          GH_TOKEN: ${{ steps.dev-token.outputs.token }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          BASE_SHA: ${{ github.event.pull_request.base.sha }}
          TRUSTED_LOGINS: ${{ steps.dev-token.outputs.app-slug }},${{ steps.dev-token.outputs.app-slug }}[bot],app/${{ steps.dev-token.outputs.app-slug }},${{ steps.review-token.outputs.app-slug }},${{ steps.review-token.outputs.app-slug }}[bot],app/${{ steps.review-token.outputs.app-slug }}
        shell: bash
        run: |
          set -euo pipefail
          notify_human_blob="$(git rev-parse "${BASE_SHA}:.github/scripts/notify-human.sh")"
          apply_human_pause_blob="$(git rev-parse "${BASE_SHA}:.github/scripts/apply-human-pause.sh")"
          requirements_marker_blob="$(git rev-parse "${BASE_SHA}:.github/scripts/has-requirements-change-marker.sh")"
          diff_guard_blob="$(git rev-parse "${BASE_SHA}:.github/scripts/evaluate-codex-diff-gate.sh")"
          {
            printf 'notify_human_blob=%s\n' "$notify_human_blob"
            printf 'apply_human_pause_blob=%s\n' "$apply_human_pause_blob"
            printf 'requirements_marker_blob=%s\n' "$requirements_marker_blob"
            printf 'diff_guard_blob=%s\n' "$diff_guard_blob"
          } >> "$GITHUB_OUTPUT"
          mkdir -p .ai-context
          git show "${BASE_SHA}:.github/scripts/build-review-context.sh" > "$RUNNER_TEMP/build-review-context.sh"
          git show "${BASE_SHA}:AGENTS.md" > .ai-context/AGENTS.base.md
          git show "${BASE_SHA}:.github/scripts/apply-human-pause.sh" > "$RUNNER_TEMP/apply-human-pause.sh"
          git show "${BASE_SHA}:.github/scripts/has-requirements-change-marker.sh" > "$RUNNER_TEMP/has-requirements-change-marker.sh"
          git show "${BASE_SHA}:.github/scripts/notify-human.sh" > "$RUNNER_TEMP/notify-human.sh"
          git show "${BASE_SHA}:.github/scripts/evaluate-codex-diff-gate.sh" > "$RUNNER_TEMP/evaluate-codex-diff-gate.sh"
          test "$(git hash-object --no-filters "$RUNNER_TEMP/notify-human.sh")" = "$notify_human_blob"
          test "$(git hash-object --no-filters "$RUNNER_TEMP/apply-human-pause.sh")" = "$apply_human_pause_blob"
          test "$(git hash-object --no-filters "$RUNNER_TEMP/has-requirements-change-marker.sh")" = "$requirements_marker_blob"
          test "$(git hash-object --no-filters "$RUNNER_TEMP/evaluate-codex-diff-gate.sh")" = "$diff_guard_blob"
          bash "$RUNNER_TEMP/evaluate-codex-diff-gate.sh" --contract > "$RUNNER_TEMP/codex-diff-guard-contract.json"
          jq -e '
            type == "object" and
            (keys | sort == ["max_changed_files", "max_changed_lines", "max_new_files"]) and
            (.max_changed_files | type == "number" and . > 0 and floor == .) and
            (.max_changed_lines | type == "number" and . > 0 and floor == .) and
            (.max_new_files | type == "number" and . > 0 and floor == .)
          ' "$RUNNER_TEMP/codex-diff-guard-contract.json" > /dev/null
          cp "$RUNNER_TEMP/codex-diff-guard-contract.json" .ai-context/diff-guard-contract.json
          bash "$RUNNER_TEMP/build-review-context.sh" "$GITHUB_REPOSITORY" "$PR_NUMBER" .ai-context/request.md "$TRUSTED_LOGINS"

          # Do not carry trusted post-Codex helpers across model execution.
          rm -f -- \
            "$RUNNER_TEMP/build-review-context.sh" \
            "$RUNNER_TEMP/notify-human.sh" \
            "$RUNNER_TEMP/apply-human-pause.sh" \
            "$RUNNER_TEMP/has-requirements-change-marker.sh" \
            "$RUNNER_TEMP/evaluate-codex-diff-gate.sh" \
            "$RUNNER_TEMP/codex-diff-guard-contract.json"

      - name: Validate Codex model configuration
        if: steps.verify-reviewer.outputs.trusted == 'true' && steps.followup-gate.outputs.continue == 'true'
        env:
          CODEX_MODEL: ${{ vars.CODEX_MODEL }}
        shell: bash
        run: |
          set -euo pipefail
          if [[ ! "$CODEX_MODEL" =~ [^[:space:]] ]]; then
            echo 'CODEX_MODEL repository variable must contain a non-whitespace value.' >&2
            exit 1
          fi

      - name: Prepare Codex follow-up runtime
        if: steps.verify-reviewer.outputs.trusted == 'true' && steps.followup-gate.outputs.continue == 'true'
        env:
          CODEX_HOME: ${{ runner.temp }}/codex-home
          CODEX_FINAL: ${{ runner.temp }}/codex-final.md
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p "$CODEX_HOME"
          rm -f "$CODEX_HOME/config.toml"
          rm -f "$CODEX_FINAL"

      - name: Setup Codex follow-up runtime
        if: steps.verify-reviewer.outputs.trusted == 'true' && steps.followup-gate.outputs.continue == 'true'
        uses: openai/codex-action@86365089eb2b84e0a8fb0717b304f8bdcb13b20e # v1.12
        with:
          openai-api-key: ${{ secrets.OPENAI_API_KEY }}
          codex-version: 0.156.1
          codex-home: ${{ runner.temp }}/codex-home
          safety-strategy: unsafe
          allow-bot-users: ${{ steps.review-token.outputs.app-slug }}

      - name: Resolve trusted Codex follow-up runtime
        if: steps.verify-reviewer.outputs.trusted == 'true' && steps.followup-gate.outputs.continue == 'true'
        id: followup_codex_runtime
        timeout-minutes: 3
        env:
          CODEX_HOME: ${{ runner.temp }}/codex-home
        shell: bash
        run: |
          set -euo pipefail

          launcher="$(command -v codex)"
          test -n "$launcher"
          entry="$(realpath "$launcher")"
          test "$(basename "$entry")" = codex.js
          package_root="$(realpath "$(dirname "$(dirname "$entry")")")"
          test -s "$package_root/package.json"
          test "$(realpath "$package_root/bin/codex.js")" = "$entry"

          export CODEX_ENTRY="$entry"
          export CODEX_PACKAGE_ROOT="$package_root"
          resolver_output="$(
            node --input-type=module <<'NODE'
          import fs from "node:fs";
          import path from "node:path";
          import { createRequire } from "node:module";

          const entry = process.env.CODEX_ENTRY;
          const packageRoot = process.env.CODEX_PACKAGE_ROOT;
          const mainPackage = JSON.parse(
            fs.readFileSync(path.join(packageRoot, "package.json"), "utf8"),
          );
          if (mainPackage.name !== "@openai/codex") {
            throw new Error(`unexpected package name: ${mainPackage.name}`);
          }
          if (mainPackage.version !== "0.156.1") {
            throw new Error(`unexpected package version: ${mainPackage.version}`);
          }

          let platformPackage;
          let targetTriple;
          if (process.platform === "linux" && process.arch === "x64") {
            platformPackage = "@openai/codex-linux-x64";
            targetTriple = "x86_64-unknown-linux-musl";
          } else if (process.platform === "linux" && process.arch === "arm64") {
            platformPackage = "@openai/codex-linux-arm64";
            targetTriple = "aarch64-unknown-linux-musl";
          } else {
            throw new Error(
              `unsupported Codex platform: ${process.platform}/${process.arch}`,
            );
          }

          const require = createRequire(entry);
          const platformPackageJson = require.resolve(
            `${platformPackage}/package.json`,
          );
          const nativePath = path.join(
            path.dirname(platformPackageJson),
            "vendor",
            targetTriple,
            "bin",
            "codex",
          );
          const nativeStat = fs.statSync(nativePath);
          if (!nativeStat.isFile()) {
            throw new Error("native codex is not a regular file");
          }
          fs.accessSync(nativePath, fs.constants.X_OK);

          process.stdout.write(
            JSON.stringify({
              nativePath: fs.realpathSync(nativePath),
              packageRoot,
              targetTriple,
            }),
          );
          NODE
          )"

          native_path="$(jq -r '.nativePath' <<<"$resolver_output")"
          resolved_root="$(jq -r '.packageRoot' <<<"$resolver_output")"
          target_triple="$(jq -r '.targetTriple' <<<"$resolver_output")"
          test "$resolved_root" = "$package_root"
          test -x "$native_path"

          launcher_version="$(codex --version)"
          native_version="$(
            env \
              -u CODEX_MANAGED_BY_BUN \
              -u CODEX_MANAGED_BY_PNPM \
              -u CODEX_MANAGED_BY_VITE_PLUS \
              CODEX_MANAGED_PACKAGE_ROOT="$package_root" \
              CODEX_MANAGED_BY_NPM=1 \
              "$native_path" --version
          )"
          test "$launcher_version" = "$native_version"
          test "$native_version" = 'codex-cli 0.156.1'

          action_root="$(dirname "$RUNNER_WORKSPACE")/_actions/openai/codex-action/86365089eb2b84e0a8fb0717b304f8bdcb13b20e"
          action_main="$action_root/dist/main.js"
          test -f "$action_main"
          actual_blob="$(git hash-object "$action_main")"
          test "$actual_blob" = ce4e94e119abb91b980d23bfb4210688241f3a0a

          uid="$(id -u)"
          gid="$(id -g)"
          groups_json="$(id -G | tr ' ' '\n' | jq -Rsc 'split("\n") | map(select(length > 0) | tonumber)')"
          credentials="$(jq -cn \
            --argjson uid "$uid" \
            --argjson gid "$gid" \
            --argjson groups "$groups_json" \
            '{userId:$uid,primaryGroupId:$gid,supplementaryGroupIds:$groups}')"

          printf 'native_path=%s\n' "$native_path" >> "$GITHUB_OUTPUT"
          printf 'package_root=%s\n' "$package_root" >> "$GITHUB_OUTPUT"
          printf 'action_main=%s\n' "$action_main" >> "$GITHUB_OUTPUT"
          printf 'runner_credentials=%s\n' "$credentials" >> "$GITHUB_OUTPUT"
          printf 'Resolved trusted Codex 0.156.1 runtime for %s (Action blob %s).\n' \
            "$target_triple" "$actual_blob"

      - name: Prepare fixed Codex follow-up prompt
        if: steps.verify-reviewer.outputs.trusted == 'true' && steps.followup-gate.outputs.continue == 'true'
        timeout-minutes: 3
        env:
          CODEX_PROMPT_FILE: ${{ runner.temp }}/codex-followup-prompt.md
        shell: bash
        run: |
          set -euo pipefail
          cat > "$CODEX_PROMPT_FILE" <<'CODEX_PROMPT'
          Read .ai-context/AGENTS.base.md, .ai-context/request.md, and .ai-context/diff-guard-contract.json completely. The base-commit AGENTS copy is the governing instruction file.
          Address every blocking Claude finding in this working tree.
          Before editing, inspect every blocking finding against repository and supplied Issue evidence. If any finding requires a human requirement or upstream-phase decision, leave the entire working tree unchanged, do not mix in fixes for other findings, and follow the exact standalone [REQUIREMENTS_CHANGE_REQUIRED] marker contract in AGENTS.base.md.
          Otherwise, correct every valid in-scope finding and all directly affected authoritative artifacts; validate the result. Explain any finding not implemented with concrete repository or Issue evidence. Do not silently change requirements to satisfy a finding.
          The trusted contract sets maximum changed files, total changed lines (additions + deletions), and new files; keep every metric at or below its maximum.
          Keep the proposed repository change within the trusted diff guard contract. Avoid broad formatting changes and large generated additions. If the work cannot fit within those bounds, keep the change small and explain the required split in the final response.
          Do not commit, push, comment, merge, or contact external services; the workflow handles GitHub writes after you finish.
          CODEX_PROMPT

      - name: Capture Codex follow-up host integrity baseline
        if: steps.verify-reviewer.outputs.trusted == 'true' && steps.followup-gate.outputs.continue == 'true'
        id: followup_host_integrity_before
        timeout-minutes: 1
        shell: bash
        run: |
          set -euo pipefail

          notify_before="$(stat -Lc '%d %i %u %g %a' /run/systemd/notify)"
          dbus_before="$(stat -Lc '%d %i %u %g %a' /run/dbus/system_bus_socket)"
          resolved_before="$(
            systemctl show systemd-resolved.service --no-pager \
              --property=ActiveState --property=SubState --property=MainPID --property=NRestarts |
              LC_ALL=C sort |
              tr '\n' ';'
          )"
          printf '%s\n' "$resolved_before" | tr ';' '\n' | grep -Fxq 'ActiveState=active'
          printf '%s\n' "$resolved_before" | tr ';' '\n' | grep -Fxq 'SubState=running'
          getent ahosts github.com >/dev/null
          getent ahosts api.github.com >/dev/null

          printf 'notify=%s\n' "$notify_before" >> "$GITHUB_OUTPUT"
          printf 'dbus=%s\n' "$dbus_before" >> "$GITHUB_OUTPUT"
          printf 'resolved=%s\n' "$resolved_before" >> "$GITHUB_OUTPUT"
          printf 'HOST_INTEGRITY before sockets=captured resolved=active/running dns=ok\n'

      - name: Run Codex follow-up
        if: steps.verify-reviewer.outputs.trusted == 'true' && steps.followup-gate.outputs.continue == 'true'
        id: codex
        timeout-minutes: 12
        env:
          CODEX_HOME: ${{ runner.temp }}/codex-home
          CODEX_FINAL: ${{ runner.temp }}/codex-final.md
          CODEX_PROMPT_FILE: ${{ runner.temp }}/codex-followup-prompt.md
          CODEX_MODEL: ${{ vars.CODEX_MODEL }}
          CODEX_INTERNAL_ORIGINATOR_OVERRIDE: codex_github_action
          CODEX_NATIVE: ${{ steps.followup_codex_runtime.outputs.native_path }}
          CODEX_PACKAGE_ROOT: ${{ steps.followup_codex_runtime.outputs.package_root }}
          ACTION_MAIN: ${{ steps.followup_codex_runtime.outputs.action_main }}
          RUNNER_CREDENTIALS: ${{ steps.followup_codex_runtime.outputs.runner_credentials }}
          CODEX_RUNTIME_MAX_SEC: 700
        shell: bash
        run: |
          set -euo pipefail

          test -x "$CODEX_NATIVE"
          test -f "$ACTION_MAIN"
          test "$(git hash-object "$ACTION_MAIN")" = ce4e94e119abb91b980d23bfb4210688241f3a0a
          test -s "$CODEX_HOME/config.toml"
          test -s "$CODEX_PROMPT_FILE"

          python3 - "$CODEX_HOME/config.toml" <<'PY'
          import pathlib
          import sys
          import tomllib
          import urllib.parse

          config_path = pathlib.Path(sys.argv[1])
          with config_path.open("rb") as fh:
              config = tomllib.load(fh)

          allowed_top_level = {"model_provider", "model_providers"}
          unexpected = sorted(set(config) - allowed_top_level)
          if unexpected:
              raise SystemExit(
                  f"unexpected Codex config keys in setup-only runtime: {unexpected}"
              )

          provider_name = config.get("model_provider")
          if provider_name != "codex-action-responses-proxy":
              raise SystemExit("unexpected Codex model_provider")

          providers = config.get("model_providers")
          if not isinstance(providers, dict) or set(providers) != {provider_name}:
              raise SystemExit("unexpected Codex model_providers shape")
          provider = providers[provider_name]
          if not isinstance(provider, dict):
              raise SystemExit("unexpected Codex provider config")
          if provider.get("wire_api") != "responses":
              raise SystemExit("Codex proxy provider must use responses wire API")

          parsed = urllib.parse.urlparse(str(provider.get("base_url", "")))
          if (
              parsed.scheme != "http"
              or parsed.hostname != "127.0.0.1"
              or parsed.port is None
              or not (1 <= parsed.port <= 65535)
              or parsed.path != "/v1"
              or parsed.params
              or parsed.query
              or parsed.fragment
              or parsed.username is not None
              or parsed.password is not None
          ):
              raise SystemExit("Codex proxy provider must be loopback http /v1")
          PY

          case "$CODEX_RUNTIME_MAX_SEC" in
            700) ;;
            *)
              echo 'Unexpected Codex RuntimeMaxSec.' >&2
              exit 1
              ;;
          esac

          uid="$(id -u)"
          gid="$(id -g)"
          groups_json="$(id -G | tr ' ' '\n' | jq -Rsc 'split("\n") | map(select(length > 0) | tonumber)')"
          current_credentials="$(jq -cn \
            --argjson uid "$uid" \
            --argjson gid "$gid" \
            --argjson groups "$groups_json" \
            '{userId:$uid,primaryGroupId:$gid,supplementaryGroupIds:$groups}')"
          test "$current_credentials" = "$RUNNER_CREDENTIALS"
          test "$uid" -ne 0

          nobody_gid="$(id -g nobody)"
          case "$nobody_gid" in
            ''|*[!0-9]*|0)
              echo 'Could not resolve a safe nobody gid.' >&2
              exit 1
              ;;
          esac

          runner_user="$(id -un)"
          runner_home="$HOME"
          runner_path="$PATH"
          unit="codex-followup-${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}"

          cat > "$RUNNER_TEMP/run-native-codex.sh" <<'CODEX_RUN'
          #!/bin/sh
          set -eu

          expected_uid="${1:?expected uid is required}"
          expected_gid="${2:?expected gid is required}"

          actual_uid="$(/usr/bin/id -u)"
          if [ "$actual_uid" != "$expected_uid" ]; then
            echo "Service-local hardening preflight UID mismatch: expected $expected_uid, got $actual_uid." >&2
            exit 41
          fi

          actual_gid="$(/usr/bin/id -g)"
          if [ "$actual_gid" != "$expected_gid" ]; then
            echo "Service-local hardening preflight GID mismatch: expected $expected_gid, got $actual_gid." >&2
            exit 42
          fi

          groups_status="$(/usr/bin/awk '/^Groups:/ { $1=""; sub(/^[[:space:]]+/, ""); print }' /proc/self/status)"
          if [ -n "$groups_status" ]; then
            echo "Service-local hardening preflight retained supplementary groups: $groups_status." >&2
            exit 43
          fi

          no_new_privs="$(/usr/bin/awk '/^NoNewPrivs:/ { print $2 }' /proc/self/status)"
          if [ "$no_new_privs" != 1 ]; then
            echo "Service-local hardening preflight NoNewPrivs mismatch: got ${no_new_privs:-missing}." >&2
            exit 44
          fi

          for field in CapInh CapPrm CapEff CapBnd CapAmb; do
            value="$(/usr/bin/awk -v field="$field" '$1 == field ":" { print $2 }' /proc/self/status)"
            if [ "$value" != 0000000000000000 ]; then
              echo "Service-local hardening preflight capability is nonzero: $field=${value:-missing}." >&2
              exit 45
            fi
          done

          if [ ! -x /usr/bin/sudo ]; then
            echo 'Service-local hardening preflight cannot verify sudo rejection: /usr/bin/sudo is unavailable.' >&2
            exit 39
          fi
          if /usr/bin/sudo -n true >/dev/null 2>&1; then
            echo 'Service-local hardening preflight unexpectedly retained sudo.' >&2
            exit 40
          fi

          /usr/bin/python3 - <<'PY'
          import errno
          import os
          import socket
          import stat
          import sys

          protected_paths = os.environ.get("PROTECTED_UNIX_SOCKET_PATHS", "").split()
          if (
              len(protected_paths) != 13
              or len(set(protected_paths)) != 13
              or any(not path.startswith("/run/") for path in protected_paths)
          ):
              print(
                  "Service-local hardening preflight protected socket allowlist is malformed.",
                  file=sys.stderr,
              )
              raise SystemExit(48)

          try:
              sock = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
          except OSError as exc:
              print(
                  f"Service-local hardening preflight blocks AF_UNIX required by Codex sandbox: {exc}",
                  file=sys.stderr,
              )
              raise SystemExit(46)
          else:
              sock.close()

          try:
              sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
          except OSError as exc:
              print(
                  f"Service-local hardening preflight blocks AF_INET: {exc}",
                  file=sys.stderr,
              )
              raise SystemExit(47)
          else:
              sock.close()

          host_ids = {}
          raw_host_ids = os.environ.get("PROTECTED_UNIX_SOCKET_HOST_IDS", "")
          if raw_host_ids:
              try:
                  for item in raw_host_ids.split(";"):
                      path, devino = item.split("=", 1)
                      dev, ino = (int(part) for part in devino.split(":", 1))
                      if path in host_ids:
                          raise ValueError("duplicate protected socket host id")
                      host_ids[path] = (dev, ino)
              except (TypeError, ValueError):
                  print(
                      "Service-local hardening preflight protected socket host baseline is malformed.",
                      file=sys.stderr,
                  )
                  raise SystemExit(48)
          if any(path not in protected_paths for path in host_ids):
              print(
                  "Service-local hardening preflight protected socket host baseline has an unexpected path.",
                  file=sys.stderr,
              )
              raise SystemExit(48)

          for path in protected_paths:
              try:
                  st = os.stat(path)
              except FileNotFoundError:
                  continue
              except OSError as exc:
                  print(
                      f"Service-local hardening preflight cannot inspect protected socket {path}: {exc}",
                      file=sys.stderr,
                  )
                  raise SystemExit(48)

              if path not in host_ids:
                  print(
                      f"Service-local hardening preflight protected socket appeared after host baseline: {path}.",
                      file=sys.stderr,
                  )
                  raise SystemExit(48)

              if (
                  not stat.S_ISSOCK(st.st_mode)
                  or stat.S_IMODE(st.st_mode) != 0
                  or os.access(path, os.R_OK)
                  or os.access(path, os.W_OK)
                  or os.access(path, os.X_OK)
                  or (st.st_dev, st.st_ino) == host_ids[path]
              ):
                  print(
                      f"Service-local hardening preflight protected socket mask is ineffective: {path}.",
                      file=sys.stderr,
                  )
                  raise SystemExit(48)

          def handle_walk_error(exc):
              if exc.errno in (errno.EACCES, errno.EPERM, errno.ENOENT):
                  return
              raise exc

          residual = []
          try:
              for root, _dirs, files in os.walk(
                  "/run",
                  topdown=True,
                  onerror=handle_walk_error,
                  followlinks=False,
              ):
                  for name in files:
                      path = os.path.join(root, name)
                      try:
                          st = os.stat(path, follow_symlinks=False)
                      except (FileNotFoundError, PermissionError):
                          continue
                      if (
                          stat.S_ISSOCK(st.st_mode)
                          and st.st_uid == 0
                          and os.access(path, os.W_OK)
                      ):
                          residual.append(path)
          except OSError as exc:
              print(
                  f"Service-local hardening preflight failed while scanning /run: {exc}",
                  file=sys.stderr,
              )
              raise SystemExit(49)

          if residual:
              rendered = ", ".join(sorted(set(residual))[:20])
              print(
                  "Service-local hardening preflight found writable root-owned UNIX socket(s): "
                  + rendered,
                  file=sys.stderr,
              )
              raise SystemExit(49)

          print(
              "Service-local hardening preflight verified AF_UNIX/AF_INET and protected UNIX socket boundary."
          )
          PY

          exec env \
            -u PROTECTED_UNIX_SOCKET_PATHS \
            -u PROTECTED_UNIX_SOCKET_HOST_IDS \
            -u CODEX_MANAGED_BY_BUN \
            -u CODEX_MANAGED_BY_PNPM \
            -u CODEX_MANAGED_BY_VITE_PLUS \
            CODEX_MANAGED_PACKAGE_ROOT="$CODEX_PACKAGE_ROOT" \
            CODEX_MANAGED_BY_NPM=1 \
            CODEX_HOME="$CODEX_HOME" \
            CODEX_INTERNAL_ORIGINATOR_OVERRIDE="$CODEX_INTERNAL_ORIGINATOR_OVERRIDE" \
            "$CODEX_NATIVE" exec \
              --skip-git-repo-check \
              --cd "$GITHUB_WORKSPACE" \
              --output-last-message "$CODEX_FINAL" \
              --model "$CODEX_MODEL" \
              --config 'model_reasoning_effort="medium"' \
              --config 'default_permissions=":workspace"' \
              < "$CODEX_PROMPT_FILE"
          CODEX_RUN
          chmod 700 "$RUNNER_TEMP/run-native-codex.sh"

          exec sudo -n -- \
            env -u ENV -u BASH_ENV -u SHELLOPTS \
            /bin/sh -c '
              set -eu
              runner_user="$1"
              uid="$2"
              nobody_gid="$3"
              runner_home="$4"
              runner_path="$5"
              runner_temp="$6"
              runtime_max_sec="$7"
              unit="$8"
              launcher="$9"
              code_workspace="${10}"
              codex_home="${11}"
              codex_final="${12}"
              codex_prompt_file="${13}"
              codex_model="${14}"
              codex_native="${15}"
              codex_package_root="${16}"
              originator="${17}"
              protected_unix_socket_paths="/run/dbus/system_bus_socket /run/dhcpcd/eth0-4.unpriv.sock /run/docker.sock /run/snapd-snap.socket /run/snapd.socket /run/systemd/io.systemd.ManagedOOM /run/systemd/journal/dev-log /run/systemd/journal/socket /run/systemd/journal/stdout /run/systemd/journal/syslog /run/systemd/notify /run/systemd/userdb/io.systemd.DynamicUser /run/uuidd/request"
              inaccessible_paths=""
              protected_unix_socket_host_ids=""
              for path in $protected_unix_socket_paths; do
                inaccessible_paths="${inaccessible_paths:+$inaccessible_paths }-$path"
                if [ -e "$path" ]; then
                  if [ ! -S "$path" ]; then
                    echo "Service-local hardening root preflight protected UNIX socket baseline failed: $path is not a socket." >&2
                    exit 50
                  fi
                  if ! host_owner="$(/usr/bin/stat -Lc "%u" "$path")"; then
                    echo "Service-local hardening root preflight protected UNIX socket baseline failed: could not stat owner for $path." >&2
                    exit 50
                  fi
                  if ! host_devino="$(/usr/bin/stat -Lc "%d:%i" "$path")"; then
                    echo "Service-local hardening root preflight protected UNIX socket baseline failed: could not stat dev:inode for $path." >&2
                    exit 50
                  fi
                  if [ "$host_owner" != 0 ]; then
                    echo "Service-local hardening root preflight protected UNIX socket baseline failed: $path owner=$host_owner." >&2
                    exit 50
                  fi
                  protected_unix_socket_host_ids="${protected_unix_socket_host_ids}${protected_unix_socket_host_ids:+;}${path}=${host_devino}"
                fi
              done

              set +e
              /usr/bin/systemd-run \
                --quiet \
                --wait \
                --collect \
                --unit="$unit" \
                --property=Type=exec \
                --property="RuntimeMaxSec=${runtime_max_sec}s" \
                --property=TimeoutStopSec=5s \
                --property=KillMode=control-group \
                --property=SendSIGKILL=yes \
                --property=NoNewPrivileges=yes \
                --property="InaccessiblePaths=$inaccessible_paths" \
                --property=SystemCallArchitectures=native \
                --property="SystemCallFilter=~io_uring_setup io_uring_enter io_uring_register" \
                /usr/bin/setpriv \
                  --reuid="$uid" \
                  --regid="$nobody_gid" \
                  --clear-groups \
                  --no-new-privs \
                  --bounding-set=-all \
                  --inh-caps=-all \
                  --ambient-caps=-all \
                  -- /usr/bin/env -i \
                  "HOME=$runner_home" \
                  "USER=$runner_user" \
                  "LOGNAME=$runner_user" \
                  "PATH=$runner_path" \
                  "RUNNER_TEMP=$runner_temp" \
                  "GITHUB_WORKSPACE=$code_workspace" \
                  "CODEX_HOME=$codex_home" \
                  "CODEX_FINAL=$codex_final" \
                  "CODEX_PROMPT_FILE=$codex_prompt_file" \
                  "CODEX_MODEL=$codex_model" \
                  "CODEX_NATIVE=$codex_native" \
                  "CODEX_PACKAGE_ROOT=$codex_package_root" \
                  "CODEX_INTERNAL_ORIGINATOR_OVERRIDE=$originator" \
                  "PROTECTED_UNIX_SOCKET_PATHS=$protected_unix_socket_paths" \
                  "PROTECTED_UNIX_SOCKET_HOST_IDS=$protected_unix_socket_host_ids" \
                  /bin/sh "$launcher" "$uid" "$nobody_gid"
              rc=$?
              set -e

              /usr/bin/journalctl \
                --unit="$unit" \
                --no-pager \
                --output=cat \
                --lines=200 || true

              if [ "$rc" -eq 0 ]; then
                expected_preflight_marker="Service-local hardening preflight verified AF_UNIX/AF_INET and protected UNIX socket boundary."
                set +e
                preflight_journal="$(
                  /usr/bin/journalctl \
                    --unit="$unit" \
                    --no-pager \
                    --output=cat \
                    --quiet
                )"
                journal_rc=$?
                set -e
                if [ "$journal_rc" -ne 0 ] || ! printf "%s\n" "$preflight_journal" | grep -Fxq "$expected_preflight_marker"; then
                  echo "Service-local hardening preflight success marker unavailable from unit journal." >&2
                  exit 51
                fi
                printf "%s\n" "$expected_preflight_marker"
              fi
              exit "$rc"
            ' codex-followup \
            "$runner_user" \
            "$uid" \
            "$nobody_gid" \
            "$runner_home" \
            "$runner_path" \
            "$RUNNER_TEMP" \
            "$CODEX_RUNTIME_MAX_SEC" \
            "$unit" \
            "$RUNNER_TEMP/run-native-codex.sh" \
            "$GITHUB_WORKSPACE" \
            "$CODEX_HOME" \
            "$CODEX_FINAL" \
            "$CODEX_PROMPT_FILE" \
            "$CODEX_MODEL" \
            "$CODEX_NATIVE" \
            "$CODEX_PACKAGE_ROOT" \
            "$CODEX_INTERNAL_ORIGINATOR_OVERRIDE"

      - name: Verify Codex follow-up host integrity
        if: always() && steps.verify-reviewer.outputs.trusted == 'true' && steps.followup-gate.outputs.continue == 'true'
        timeout-minutes: 1
        env:
          HOST_NOTIFY_BEFORE: ${{ steps.followup_host_integrity_before.outputs.notify }}
          HOST_DBUS_BEFORE: ${{ steps.followup_host_integrity_before.outputs.dbus }}
          HOST_RESOLVED_BEFORE: ${{ steps.followup_host_integrity_before.outputs.resolved }}
        shell: bash
        run: |
          set -euo pipefail

          test -n "$HOST_NOTIFY_BEFORE"
          test -n "$HOST_DBUS_BEFORE"
          test -n "$HOST_RESOLVED_BEFORE"

          notify_after="$(stat -Lc '%d %i %u %g %a' /run/systemd/notify)"
          dbus_after="$(stat -Lc '%d %i %u %g %a' /run/dbus/system_bus_socket)"
          test "$notify_after" = "$HOST_NOTIFY_BEFORE"
          test "$dbus_after" = "$HOST_DBUS_BEFORE"

          resolved_after="$(
            systemctl show systemd-resolved.service --no-pager \
              --property=ActiveState --property=SubState --property=MainPID --property=NRestarts |
              LC_ALL=C sort |
              tr '\n' ';'
          )"
          test "$resolved_after" = "$HOST_RESOLVED_BEFORE"
          printf '%s\n' "$resolved_after" | tr ';' '\n' | grep -Fxq 'ActiveState=active'
          printf '%s\n' "$resolved_after" | tr ';' '\n' | grep -Fxq 'SubState=running'
          getent ahosts github.com >/dev/null
          getent ahosts api.github.com >/dev/null
          printf 'HOST_INTEGRITY after sockets=unchanged resolved=unchanged dns=ok\n'

      - name: Restore trusted post-Codex helpers
        if: steps.verify-reviewer.outputs.trusted == 'true' && steps.followup-gate.outputs.continue == 'true'
        env:
          BASE_SHA: ${{ github.event.pull_request.base.sha }}
          NOTIFY_HUMAN_BLOB: ${{ steps.followup_context.outputs.notify_human_blob }}
          APPLY_HUMAN_PAUSE_BLOB: ${{ steps.followup_context.outputs.apply_human_pause_blob }}
          REQUIREMENTS_MARKER_BLOB: ${{ steps.followup_context.outputs.requirements_marker_blob }}
          DIFF_GUARD_BLOB: ${{ steps.followup_context.outputs.diff_guard_blob }}
        shell: bash
        run: |
          set -euo pipefail

          restore_base_blob() {
            local source_path="${1:?source path is required}"
            local destination="${2:?destination is required}"
            local expected_blob="${3:?expected blob is required}"
            if [[ ! "$expected_blob" =~ ^[0-9a-f]{40}$ ]]; then
              echo "Invalid trusted helper blob identity for $source_path." >&2
              exit 1
            fi
            rm -f -- "$destination"
            git show "${BASE_SHA}:${source_path}" > "$destination"
            actual_blob="$(git hash-object --no-filters "$destination")"
            if [ "$actual_blob" != "$expected_blob" ]; then
              echo "Trusted helper blob changed across Codex execution: $source_path" >&2
              exit 1
            fi
          }

          restore_base_blob '.github/scripts/notify-human.sh' \
            "$RUNNER_TEMP/notify-human.sh" "$NOTIFY_HUMAN_BLOB"
          restore_base_blob '.github/scripts/apply-human-pause.sh' \
            "$RUNNER_TEMP/apply-human-pause.sh" "$APPLY_HUMAN_PAUSE_BLOB"
          restore_base_blob '.github/scripts/has-requirements-change-marker.sh' \
            "$RUNNER_TEMP/has-requirements-change-marker.sh" "$REQUIREMENTS_MARKER_BLOB"
          restore_base_blob '.github/scripts/evaluate-codex-diff-gate.sh' \
            "$RUNNER_TEMP/evaluate-codex-diff-gate.sh" "$DIFF_GUARD_BLOB"

          trusted_pause_dir="$RUNNER_TEMP/trusted-human-pause"
          mkdir -p "$trusted_pause_dir"
          for helper in create-human-pause human-pause-record list-human-pause-records \
            validate-human-pause-record-graph decompose-human-pause-record-graph \
            derive-human-pause-pre-resume-state reconcile-human-pause-resume-acceptance \
            reconcile-human-pause-active-pause apply-human-pause \
            format-human-pause-notification notify-human; do
            git show "${BASE_SHA}:.github/scripts/${helper}.sh" > "$trusted_pause_dir/${helper}.sh"
            test "$(git hash-object --no-filters "$trusted_pause_dir/${helper}.sh")" = \
              "$(git rev-parse "${BASE_SHA}:.github/scripts/${helper}.sh")"
          done

          rm -f -- "$RUNNER_TEMP/codex-diff-guard-contract.json"
          bash "$RUNNER_TEMP/evaluate-codex-diff-gate.sh" --contract > "$RUNNER_TEMP/codex-diff-guard-contract.json"
          jq -e '
            type == "object" and
            (keys | sort == ["max_changed_files", "max_changed_lines", "max_new_files"]) and
            (.max_changed_files | type == "number" and . > 0 and floor == .) and
            (.max_changed_lines | type == "number" and . > 0 and floor == .) and
            (.max_new_files | type == "number" and . > 0 and floor == .)
          ' "$RUNNER_TEMP/codex-diff-guard-contract.json" > /dev/null
          echo 'TRUSTED_POST_CODEX_HELPERS restored base blobs and regenerated diff guard contract.'

      - name: Gate Codex follow-up requirement changes
        if: steps.verify-reviewer.outputs.trusted == 'true' && steps.followup-gate.outputs.continue == 'true'
        id: codex-requirements-gate
        env:
          GH_TOKEN: ${{ steps.dev-token.outputs.token }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          CODEX_FINAL: ${{ runner.temp }}/codex-final.md
          HEAD_REF: ${{ github.event.pull_request.head.ref }}
          EVENT_HEAD: ${{ github.event.pull_request.head.sha }}
          APP_SLUG: ${{ steps.dev-token.outputs.app-slug }}
          NOTIFICATION_WEBHOOK_URL: ${{ secrets.NOTIFICATION_WEBHOOK_URL }}
        shell: bash
        run: |
          set -euo pipefail
          pause_for_human() {
            local pause_reason="${1:?pause reason is required}"
            local detail="${2:?pause detail is required}"
            local options=() fingerprint current_head
            if [[ "$HEAD_REF" =~ ^ai/issue-([0-9]+)$ ]]; then
              issue_number="${BASH_REMATCH[1]}"
            else
              issue_number='-'
            fi
            if [ "$pause_reason" = requirements_change ]; then
              [ "$issue_number" != '-' ] || exit 1
              fingerprint="$(gh api "repos/${GITHUB_REPOSITORY}/issues/${issue_number}" | python3 -c '
          import hashlib, json, sys
          body = json.load(sys.stdin)["body"]
          if not isinstance(body, str): raise ValueError("invalid Issue body")
          print("sha256:" + hashlib.sha256(body.encode()).hexdigest())
          ')"
              options=(--issue-body-fingerprint "$fingerprint")
            elif [ "$pause_reason" = developer_execution_failed ]; then
              current_head="$(gh pr view "$PR_NUMBER" --repo "$GITHUB_REPOSITORY" --json headRefOid --jq .headRefOid)"
              if [ "$current_head" = "$EVENT_HEAD" ]; then
                options=(--paused-head "$current_head" --failed-action fix)
              else
                pause_reason=state_inconsistent
              fi
            fi
            app_id="$(gh api "/apps/$APP_SLUG" --jq .id)"
            bash "$RUNNER_TEMP/trusted-human-pause/create-human-pause.sh" create \
              "$GITHUB_REPOSITORY" "$issue_number" "$PR_NUMBER" "$app_id" \
              "$pause_reason" "$detail" "${options[@]}"
            echo 'continue=false' >> "$GITHUB_OUTPUT"
          }
          if [ ! -s "$CODEX_FINAL" ]; then
            pause_for_human developer_execution_failed 'Codex final response is missing; automated follow-up is paused pending a human decision.'
            exit 0
          fi
          set +e
          bash "$RUNNER_TEMP/has-requirements-change-marker.sh" "$CODEX_FINAL"
          marker_status=$?
          set -e
          if [ "$marker_status" -gt 1 ]; then
            pause_for_human state_inconsistent 'Requirements-change marker helper failed; automated follow-up is paused pending a human decision.'
            exit 0
          elif [ "$marker_status" -eq 0 ]; then
            pause_for_human requirements_change 'Codex detected that a requirements change is required. Automated follow-up is paused for a human decision.'
          else
            echo 'continue=true' >> "$GITHUB_OUTPUT"
          fi

      - name: Evaluate trusted follow-up diff guard
        if: steps.verify-reviewer.outputs.trusted == 'true' && steps.followup-gate.outputs.continue == 'true' && steps.codex-requirements-gate.outputs.continue == 'true'
        id: followup-diff-guard
        env:
          GH_TOKEN: ${{ steps.dev-token.outputs.token }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          HEAD_REF: ${{ github.event.pull_request.head.ref }}
          APP_SLUG: ${{ steps.dev-token.outputs.app-slug }}
          NOTIFICATION_WEBHOOK_URL: ${{ secrets.NOTIFICATION_WEBHOOK_URL }}
        shell: bash
        run: |
          set -euo pipefail
          rm -rf .ai-context
          git reset -- .ai-context
          git add -A

          if ! jq -e '
            type == "object" and
            (keys | sort == ["max_changed_files", "max_changed_lines", "max_new_files"]) and
            (.max_changed_files | type == "number" and . > 0 and floor == .) and
            (.max_changed_lines | type == "number" and . > 0 and floor == .) and
            (.max_new_files | type == "number" and . > 0 and floor == .)
          ' "$RUNNER_TEMP/codex-diff-guard-contract.json" > /dev/null; then
            echo 'Trusted follow-up diff guard contract is unavailable or malformed.' >&2
            exit 1
          fi
          max_changed_files="$(jq -r .max_changed_files "$RUNNER_TEMP/codex-diff-guard-contract.json")"
          max_changed_lines="$(jq -r .max_changed_lines "$RUNNER_TEMP/codex-diff-guard-contract.json")"
          max_new_files="$(jq -r .max_new_files "$RUNNER_TEMP/codex-diff-guard-contract.json")"

          result_json=''
          helper_status=0
          set +e
          result_json="$(bash "$RUNNER_TEMP/evaluate-codex-diff-gate.sh")"
          helper_status=$?
          set -e

          parsed=false
          result=''
          if [ -n "$result_json" ] && jq -e '
            type == "object" and
            (.result | type == "string") and
            (.changed_files | type == "number" and . >= 0 and floor == .) and
            (.additions | type == "number" and . >= 0 and floor == .) and
            (.deletions | type == "number" and . >= 0 and floor == .) and
            (.total_changed_lines | type == "number" and . >= 0 and floor == .) and
            (.new_files | type == "number" and . >= 0 and floor == .)
          ' <<< "$result_json" > /dev/null 2>&1; then
            parsed=true
            result="$(jq -r .result <<< "$result_json")"
          fi

          if [ "$helper_status" -eq 0 ] && [ "$parsed" = true ] && [ "$result" = pass ]; then
            echo 'continue=true' >> "$GITHUB_OUTPUT"
            {
              echo '### AI Developer follow-up diff guard'
              echo
              echo '- Result: pass'
              echo "- Changed files: $(jq -r .changed_files <<< "$result_json") / ${max_changed_files}"
              echo "- Additions: $(jq -r .additions <<< "$result_json")"
              echo "- Deletions: $(jq -r .deletions <<< "$result_json")"
              echo "- Total changed lines: $(jq -r .total_changed_lines <<< "$result_json") / ${max_changed_lines}"
              echo "- New files: $(jq -r .new_files <<< "$result_json") / ${max_new_files}"
            } >> "$GITHUB_STEP_SUMMARY"
            exit 0
          fi

          reason='Trusted follow-up diff guard failed closed before repository write.'
          if [ "$helper_status" -eq 0 ] && [ "$parsed" = true ] && [ "$result" = stop ]; then
            reason='Trusted follow-up diff guard stopped an oversized repository change before commit/push/PR write.'
          elif [ "$parsed" = true ] && [ "$result" = error ]; then
            reason='Trusted follow-up diff guard could not safely measure the staged change; automated follow-up is paused.'
          elif [ "$parsed" = true ]; then
            reason="Trusted follow-up diff guard returned unexpected result '${result}'; automated follow-up is paused."
          elif [ "$helper_status" -ne 0 ]; then
            reason='Trusted follow-up diff guard helper failed without a valid machine-readable result; automated follow-up is paused.'
          else
            reason='Trusted follow-up diff guard output could not be parsed; automated follow-up is paused.'
          fi

          if [[ "$HEAD_REF" =~ ^ai/issue-([0-9]+)$ ]]; then
            issue_number="${BASH_REMATCH[1]}"
          else
            issue_number='-'
          fi
          diagnostics="$reason"
          if [ "$parsed" = true ]; then
            diagnostics+=$'\n\n'"Result: ${result}."
            if [ "$result" = stop ]; then
              for field in changed_files additions deletions total_changed_lines new_files; do
                value="$(jq -r --arg field "$field" '.[$field]' <<< "$result_json")"
                diagnostics+=$'\n'"${field}: ${value}"
              done
            else
              if [ "$result" = error ]; then
                error_code="$(jq -r 'if (.error | type) == "string" then .error else empty end' <<< "$result_json")"
                if [ -n "$error_code" ]; then
                  diagnostics+=$'\n'"error: ${error_code}"
                fi
              fi
              diagnostics+=$'\n'"Metrics: unavailable"
            fi
          else
            diagnostics+=$'\n\n'"Result: unavailable."
            diagnostics+=$'\n'"Metrics: unavailable"
          fi
          pause_reason=diff_guard_error
          options=()
          if [ "$result" = stop ] && [ "$parsed" = true ]; then
            pause_reason=diff_guard_exceeded
            [ "$issue_number" != '-' ] || exit 1
            fingerprint="$(gh api "repos/${GITHUB_REPOSITORY}/issues/${issue_number}" | python3 -c '
          import hashlib, json, sys
          body = json.load(sys.stdin)["body"]
          if not isinstance(body, str): raise ValueError("invalid Issue body")
          print("sha256:" + hashlib.sha256(body.encode()).hexdigest())
          ')"
            options=(--issue-body-fingerprint "$fingerprint")
          fi
          app_id="$(gh api "/apps/$APP_SLUG" --jq .id)"
          bash "$RUNNER_TEMP/trusted-human-pause/create-human-pause.sh" create \
            "$GITHUB_REPOSITORY" "$issue_number" "$PR_NUMBER" "$app_id" \
            "$pause_reason" "$diagnostics" "${options[@]}"

          {
            echo '### AI Developer follow-up diff guard'
            echo
            echo "- Result: ${result:-unavailable}"
            echo "- Thresholds: ${max_changed_files} changed files / ${max_changed_lines} total changed lines / ${max_new_files} new files"
            if [ "$parsed" = true ] && [ "$result" = stop ]; then
              for field in changed_files additions deletions total_changed_lines new_files; do
                value="$(jq -r --arg field "$field" '.[$field]' <<< "$result_json")"
                echo "- ${field}: ${value}"
              done
            else
              if [ "$parsed" = true ] && [ "$result" = error ]; then
                error_code="$(jq -r 'if (.error | type) == "string" then .error else empty end' <<< "$result_json")"
                if [ -n "$error_code" ]; then
                  echo "- error: ${error_code}"
                fi
              fi
              echo '- Metrics: unavailable'
            fi
            echo '- Decision: repository write blocked'
          } >> "$GITHUB_STEP_SUMMARY"
          echo 'continue=false' >> "$GITHUB_OUTPUT"

      - name: Commit and answer review
        id: repository-write
        if: steps.verify-reviewer.outputs.trusted == 'true' && steps.followup-gate.outputs.continue == 'true' && steps.codex-requirements-gate.outputs.continue == 'true' && steps.followup-diff-guard.outputs.continue == 'true'
        env:
          GH_TOKEN: ${{ steps.dev-token.outputs.token }}
          APP_SLUG: ${{ steps.dev-token.outputs.app-slug }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          CODEX_FINAL: ${{ runner.temp }}/codex-final.md
          HEAD_REF: ${{ github.event.pull_request.head.ref }}
          EVENT_HEAD: ${{ github.event.pull_request.head.sha }}
        shell: bash
        run: |
          set -euo pipefail
          bot_login="${APP_SLUG}[bot]"
          bot_id="$(gh api "/users/${bot_login}" --jq .id)"
          git config user.name "$bot_login"
          git config user.email "${bot_id}+${bot_login}@users.noreply.github.com"

          if git diff --cached --quiet; then
            current_head="$(gh pr view "$PR_NUMBER" --repo "$GITHUB_REPOSITORY" --json headRefOid --jq .headRefOid)"
            [ "$current_head" = "$EVENT_HEAD" ] || { echo 'No-diff PR HEAD changed.' >&2; exit 1; }
            printf 'result=no_diff\nsha=%s\nstarted_at=%s\n' \
              "$current_head" "$(date -u +%s)" >> "$GITHUB_OUTPUT"
            report_file="$(mktemp)"
            {
              echo '### Validation provenance'
              echo
              echo '- No repository change was produced by this AI Developer run.'
              echo '- The validation statements in the Codex report below are Codex-reported results. They are not formal GitHub Actions evidence.'
              echo '- Formal current-head validation is recorded separately by GitHub Actions/checks. This AI Developer message does not attest their status or result.'
              echo
              echo '### Codex report'
              echo
              if [ -s "$CODEX_FINAL" ]; then cat "$CODEX_FINAL"; else echo 'Codex found no repository change and produced no final summary.'; fi
            } > "$report_file"
            gh pr comment "$PR_NUMBER" --repo "$GITHUB_REPOSITORY" --body-file "$report_file"
            rm -f "$report_file"
            exit 0
          fi

          git commit -m "Address Claude review for PR #${PR_NUMBER}"
          git push origin "HEAD:${HEAD_REF}"
          expected_head="$(git rev-parse HEAD)"
          started_at="$(date -u +%s)"
          for attempt in 1 2 3 4 5; do
            current_head="$(gh pr view "$PR_NUMBER" --repo "$GITHUB_REPOSITORY" --json headRefOid --jq .headRefOid)"
            if [ "$current_head" = "$expected_head" ]; then
              break
            fi
            if [ "$current_head" != "$EVENT_HEAD" ]; then
              echo "PR #${PR_NUMBER} head changed unexpectedly: expected ${expected_head}, observed ${current_head}." >&2
              exit 1
            fi
            if [ "$attempt" = 5 ]; then
              echo "PR #${PR_NUMBER} head did not reach pushed commit ${expected_head}." >&2
              exit 1
            fi
            sleep 2
          done
          printf 'result=pushed\nsha=%s\nstarted_at=%s\n' \
            "$expected_head" "$started_at" >> "$GITHUB_OUTPUT"
          report_file="$(mktemp)"
          {
            echo '### Validation provenance'
            echo
            echo '- The validation statements in the Codex report below are Codex-reported results. They are not formal GitHub Actions evidence.'
            echo "- Pushed commit: ${expected_head}"
            echo '- Formal current-head validation is recorded separately by GitHub Actions/checks. This AI Developer message does not attest their status or result.'
            echo
            echo '### Codex report'
            echo
            if [ -s "$CODEX_FINAL" ]; then cat "$CODEX_FINAL"; else echo 'Codex updated the branch without a final summary.'; fi
          } > "$report_file"
          gh pr comment "$PR_NUMBER" --repo "$GITHUB_REPOSITORY" --body-file "$report_file"
          rm -f "$report_file"

      - name: Ready PR for current-head traceability
        id: ready-traceability
        if: steps.repository-write.outputs.result == 'pushed' || steps.repository-write.outputs.result == 'no_diff'
        env:
          GH_TOKEN: ${{ steps.dev-token.outputs.token }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
        shell: bash
        run: |
          set -euo pipefail
          gh pr ready "$PR_NUMBER" --repo "$GITHUB_REPOSITORY"
          echo 'done=true' >> "$GITHUB_OUTPUT"

      - name: Validate current HEAD and dispatch re-review
        id: current-validation
        if: steps.repository-write.outputs.result == 'pushed' || steps.repository-write.outputs.result == 'no_diff'
        env:
          GH_TOKEN: ${{ steps.dev-token.outputs.token }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          BASE_SHA: ${{ github.event.pull_request.base.sha }}
          VALIDATED_SHA: ${{ steps.repository-write.outputs.sha }}
          WRITE_RESULT: ${{ steps.repository-write.outputs.result }}
          STARTED_AT: ${{ steps.repository-write.outputs.started_at }}
          REVIEWER_APP_SLUG: ${{ steps.review-token.outputs.app-slug }}
        shell: bash
        run: |
          set -euo pipefail
          git show "${BASE_SHA}:.github/scripts/snapshot-current-head-validation.py" > "$RUNNER_TEMP/snapshot-current-head-validation.py"
          git show "${BASE_SHA}:.github/scripts/evaluate-current-head-validation.sh" > "$RUNNER_TEMP/evaluate-current-head-validation.sh"
          round="$(gh api --paginate "repos/${GITHUB_REPOSITORY}/pulls/${PR_NUMBER}/reviews" |
            jq -s --arg slug "$REVIEWER_APP_SLUG" '[.[][] | select(.state == "CHANGES_REQUESTED" and
              (.user.login == $slug or .user.login == ($slug + "[bot]") or .user.login == ("app/" + $slug)))] | length')"
          [ "$round" -ge 1 ] && [ "$round" -le 2 ]
          deadline=$((STARTED_AT + 600))
          while :; do
            now="$(date -u +%s)"
            [ "$now" -lt "$deadline" ] || {
              echo 'code=validation_timeout' >> "$GITHUB_OUTPUT"
              echo 'Current-head validation timeout.' >&2
              exit 1
            }
            snapshot="$(python3 "$RUNNER_TEMP/snapshot-current-head-validation.py" \
              "$GITHUB_REPOSITORY" "$PR_NUMBER" "$VALIDATED_SHA" "$round" "$WRITE_RESULT" "$STARTED_AT" "$GITHUB_RUN_ID")"
            decision="$(bash "$RUNNER_TEMP/evaluate-current-head-validation.sh" <<< "$snapshot")"
            action="$(jq -r .action <<< "$decision")"
            code="$(jq -r .code <<< "$decision")"
            case "$action" in
              ready)
                [ "$code" = success ] || exit 1
                break ;;
              wait) sleep 10 ;;
              stop)
                echo "code=$code" >> "$GITHUB_OUTPUT"
                echo "Current-head validation stopped: $code" >&2
                exit 1 ;;
              *) exit 1 ;;
            esac
          done
          payload="$(jq -cn --argjson pr_number "$PR_NUMBER" --arg validated_sha "$VALIDATED_SHA" \
            --argjson round "$round" '{pr_number:$pr_number,validated_sha:$validated_sha,round:$round}')"
          echo 'code=dispatch_failed' >> "$GITHUB_OUTPUT"
          jq -cn --argjson payload "$payload" \
            '{event_type:"claude-auto-rereview",client_payload:$payload}' |
            gh api --method POST "repos/${GITHUB_REPOSITORY}/dispatches" --input -

      - name: Clear machine state after human pause
        if: always() && steps.followup-gate.outputs.continue == 'true' && (steps.codex-requirements-gate.outputs.continue == 'false' || steps.followup-diff-guard.outputs.continue == 'false')
        env:
          GH_TOKEN: ${{ steps.dev-token.outputs.token }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
        shell: bash
        run: |
          set -euo pipefail
          gh pr view "$PR_NUMBER" --repo "$GITHUB_REPOSITORY" --json labels \
            --jq '.labels | any(.name == "human-review-required")' | grep -Fxq true
          gh pr edit "$PR_NUMBER" --repo "$GITHUB_REPOSITORY" --remove-label ai-followup-in-progress

  handle-claude-followup-failure:
    name: Handle Claude follow-up failure
    needs: [draft-after-claude-changes, respond-to-claude]
    if: >-
      always() &&
      (needs.draft-after-claude-changes.result == 'failure' || needs.respond-to-claude.result == 'failure') &&
      github.event_name == 'pull_request_review' &&
      github.event.review.state == 'changes_requested' &&
      github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    permissions:
      contents: read
      issues: write
      pull-requests: write
    steps:
      - name: Create developer App token
        id: dev-token
        uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1 # v3.2.0
        with:
          client-id: ${{ vars.DEV_APP_CLIENT_ID }}
          private-key: ${{ secrets.DEV_APP_PRIVATE_KEY }}

      - name: Check out trusted automation
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          ref: ${{ github.event.pull_request.base.sha }}
          persist-credentials: false

      - name: Create common human pause
        env:
          GH_TOKEN: ${{ steps.dev-token.outputs.token }}
          APP_SLUG: ${{ steps.dev-token.outputs.app-slug }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          EVENT_HEAD: ${{ github.event.pull_request.head.sha }}
          DRAFT_RESULT: ${{ needs.draft-after-claude-changes.result }}
          FOLLOWUP_RESULT: ${{ needs.respond-to-claude.result }}
          WRITE_RESULT: ${{ needs.respond-to-claude.outputs.write_result }}
          READY_DONE: ${{ needs.respond-to-claude.outputs.ready_done }}
          VALIDATION_CODE: ${{ needs.respond-to-claude.outputs.validation_code }}
          NOTIFICATION_WEBHOOK_URL: ${{ secrets.NOTIFICATION_WEBHOOK_URL }}
        shell: bash
        run: |
          set -euo pipefail
          [[ "$PR_NUMBER" =~ ^[1-9][0-9]*$ ]]
          app_id="$(gh api "/apps/$APP_SLUG" --jq '.id')"
          [[ "$app_id" =~ ^[1-9][0-9]*$ ]]
          current_head=unknown
          if pr_json="$(gh api "/repos/$GITHUB_REPOSITORY/pulls/$PR_NUMBER")"; then
            if head="$(jq -er --arg repo "$GITHUB_REPOSITORY" --argjson pr "$PR_NUMBER" '
              select(.number == $pr and .head.repo.full_name == $repo) |
              .head.sha | select(type == "string" and test("^[0-9a-f]{40}$"))
            ' <<< "$pr_json")"; then
              current_head="$head"
            fi
          fi
          reason=state_inconsistent
          options=()
          if [[ "$EVENT_HEAD" =~ ^[0-9a-f]{40}$ ]] && [[ "$current_head" == "$EVENT_HEAD" ]]; then
            reason=developer_execution_failed
            options=(--paused-head "$current_head" --failed-action fix)
          fi
          if [ "$READY_DONE" = true ]; then
            options=()
            case "$VALIDATION_CODE" in
              validation_timeout|round_limit|validation_failed) reason="$VALIDATION_CODE" ;;
              *) reason=state_inconsistent ;;
            esac
          fi
          detail="Claude follow-up failure; draft job: ${DRAFT_RESULT}; follow-up job: ${FOLLOWUP_RESULT}; write: ${WRITE_RESULT:-missing}; validation: ${VALIDATION_CODE:-missing}; event HEAD: ${EVENT_HEAD:-missing}; current PR HEAD: ${current_head}. Run: ${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}"
          bash .github/scripts/create-human-pause.sh create \
            "$GITHUB_REPOSITORY" '-' "$PR_NUMBER" "$app_id" \
            "$reason" "$detail" "${options[@]}"
          if gh pr view "$PR_NUMBER" --repo "$GITHUB_REPOSITORY" --json labels \
              --jq '.labels | any(.name == "ai-followup-in-progress")' | grep -Fxq true; then
            gh pr edit "$PR_NUMBER" --repo "$GITHUB_REPOSITORY" --remove-label ai-followup-in-progress
          fi
