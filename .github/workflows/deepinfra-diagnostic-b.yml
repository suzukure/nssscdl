name: DeepInfra Diagnostic B

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  diagnostic-b:
    name: Run bounded selected-head Diagnostic B
    if: github.ref_name == github.event.repository.default_branch
    runs-on: ubuntu-latest
    timeout-minutes: 15
    concurrency:
      group: deepinfra-diagnostic-b
      cancel-in-progress: false
    permissions:
      contents: read
      issues: read
    steps:
      - name: Check out trusted default-branch commit
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          ref: ${{ github.sha }}
          fetch-depth: 0
          persist-credentials: false

      - name: Run Diagnostic B once
        env:
          DEEPINFRA_API_KEY: ${{ secrets.DEEPINFRA_API_KEY }}
          GH_TOKEN: ${{ github.token }}
          RESULT_JSON: ${{ runner.temp }}/deepinfra-diagnostic-b.json
          RESULT_MD: ${{ runner.temp }}/deepinfra-diagnostic-b.md
        shell: bash
        run: |
          set -euo pipefail
          test -n "$DEEPINFRA_API_KEY"
          rc=0
          python3 .github/scripts/deepinfra-diagnostic-b.py \
            --repo "$GITHUB_REPOSITORY" \
            --output-json "$RESULT_JSON" \
            --output-md "$RESULT_MD" || rc=$?
          test -s "$RESULT_JSON"
          test -s "$RESULT_MD"
          cat "$RESULT_MD" >> "$GITHUB_STEP_SUMMARY"
          exit "$rc"

      - name: Upload Diagnostic B result
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: deepinfra-diagnostic-b-${{ github.run_id }}
          path: |
            ${{ runner.temp }}/deepinfra-diagnostic-b.json
            ${{ runner.temp }}/deepinfra-diagnostic-b.md
          if-no-files-found: error
          retention-days: 7
