# PlantUML sources

このディレクトリには設計図の正本となる `.puml` ファイルを置く。

- C4図は PlantUML Standard Library の `<C4/...>` を使用する。
- 生成済みSVGは直接編集しない。
- 図の変更は `.puml` を変更して行う。
- 基本設計のC4 Level 2は `c4-container.puml` を起点とする。
